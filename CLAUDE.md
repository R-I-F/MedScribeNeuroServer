# CLAUDE.md

Guidance for Claude Code working in this repository.

## ⚠️ Critical constraints (read first)
- **NEVER commit or push to `main`.** `main` is the live **production** app. Work only on side branches (currently `migration/mysql-to-postgres`). Commit only when the user explicitly asks.
- **All database work is on STAGING**, via `.env.staging` (Aiven Postgres host `pg-288cac9e-logbooknative.g.aivencloud.com`, DB `defaultdb`). Never point scripts at production.
- Secrets `.env`, `.env.staging`, `*.pem` are gitignored. **`src/migrations/` and `scripts/` are also gitignored** — see the handoff note below.

## Project
MedScribeNeuroServer — Node/TypeScript/Express backend (TypeORM + Inversify DI), a multi-tenant medical/surgical logbook platform. Active work is on `migration/mysql-to-postgres`: migrating shared **reference data** into Postgres `defaultdb` and adding a **semantic diagnosis-search** feature exposed over **MCP**.

## Reference data model (shared, in defaultdb)
`Department → main_diags (categories) → diagnoses`, linked via `department_diagnoses` (dept↔diagnosis) and `main_diag_diagnoses` (main_diag↔diagnosis).
- Accessed by `ReferenceDataSource` (`src/config/referenceDb.config.ts`) — pooled raw-SQL connection, **no entities**.
- Goal: data should reflect **>90% of surgical diagnoses at a university hospital**. Every `main_diags` should have diagnoses (an empty one is redundant). **All ICD-11 codes must be verified** against WHO ICD-11 (findacode.com mirrors it well; the WHO browser is JS-heavy/hard to fetch) — do not guess codes.

## Semantic search + MCP
- `DiagnosisSearchService` (`src/diagnosis/diagnosisSearch.service.ts`): embeds the query (Gemini) and runs pgvector **cosine** search (`<=>`, HNSW `vector_cosine_ops`), scoped to a department.
- Embeddings: model **`gemini-embedding-001`** (the API key does NOT serve `text-embedding-004`), `outputDimensionality: 768` to match the `vector(768)` column. See `AiAgentService.embedText` (`src/aiAgent/aiAgent.service.ts`).
- MCP server (`src/mcp/`): stateless Streamable-HTTP at `/mcp`, JWT-authed, exposes a `search_diagnosis` tool.

## Staging workflow & commands
- `npm run db:migrate:staging` — apply pending migrations. **The migration list is explicit in `src/config/staging-migrations.config.ts`; add each new migration file to that array** or it won't run.
- `npm run db:backfill-embeddings:staging` — idempotent; embeds only rows with NULL `embedding` (`gemini-embedding-001`). Run after adding diagnoses.
- Per-department migration pattern: `INSERT diagnoses (EN + Arabic name + EN/AR description) … ON CONFLICT("icdCode") DO NOTHING` → link `department_diagnoses` (by `dept.code`) → link `main_diag_diagnoses` (by `md.title`). Share existing correct-coded diagnoses across departments rather than duplicating. Always include a clean `down()`.
- Department codes: CTS, GS, HBP, MFS, NS, OBGYN, OPHTHAL, ORTHO, ENT, PEDSURG, PRS, SOC, TRS, **UROL** (not URO), VASC.

## 📍 Where we stopped (2026-07-08)

### 🎨 CM dashboard conformed to the Clinical Aurora redesign (2026-07-17, uncommitted, frontend `design-integration`)
Only the candidate `DashboardLayout` had the redesign (`styles/dashboard.css`, `.app-shell`-scoped `ds-*` classes + `landing-*` tailwind tokens); CM/Supervisor/InstituteAdmin/SuperAdmin layouts were still old gray/white. **User chose: CM layout + pages full pass** (other 3 layouts still old — future passes). Done: `CalendarManagerLayout` rewritten on the DashboardLayout pattern (`.app-shell` root + `DashboardLanguageContext` provider + `LanguageToggle` + ds-sidebar/nav/chip; role badge = ds-eyebrow; prefetch/roles/nav behavior unchanged) — mounting the provider also **lit up the already-bilingual, already-ds-styled `Calendar.tsx`** (its ds classes were dead outside `.app-shell`); both CM dashboard pages + switcher + Surgeries list/form + Events list/form restyled (ds-card tables, ds-btn, ds-alert, ds-spinner, shared `inputCls`/`labelCls` field styling); `dashboard.i18n.ts` gained a `cm` block (EN+AR: roleLabel/surgeries/events). `CalSurgEditModal` restyled **self-contained (pure tailwind, NO ds-\*)** because it also mounts inside the old institute-admin layout where `.app-shell` scoping would leave ds-classes unstyled (transparent card). **Bugs found & fixed from the earlier F4 route collapse (missed by the sweep): `src/lib/pageTitles.ts` still matched old `/:institutionId`-prefixed paths** (titles/roots/back-paths all misresolved — full rewrite to param-free paths, i-admin+super-admin settings ladder unified) **and `LoginPage.isAllowedRedirect`** startsWith checks missed the new bare roots. tsc + vite build clean. ⚠️ Visual click-test by user pending (:3000 hot-reloads).
**Follow-up (same day): CM surface made FULLY bilingual** after user found EN-stuck pages — root cause was a React-context subtlety: page components render OUTSIDE the layout-mounted `DashboardLanguageContext` provider, so `useDashboardLanguageCtx()` in a page body silently returns the EN default (Calendar.tsx worked only because it's a child component). **Pattern: every CM page's visual content is now an inner `*Content` component rendered as the layout's child.** Surgeries list + create form + Events list + event form + `CalSurgEditModal` all copy-driven (~50 new `cm` keys EN+AR in `dashboard.i18n.ts`) with language-aware DATA (patientNameAr/En, clerkProc titleAr/titleEn + ALPHA, hospital arabName/engName, ar-EG date locale, `text-start/end` RTL tables). Modal reused inside i-admin (no provider) → falls back EN gracefully, styling kept self-contained (no ds-*). Reused existing AR terms (المشرف, طبيب مقيم).
**Perf fix (same day): calSurg fetches are now MONTH-SCOPED (frontend, uncommitted)** — `/calSurg/getAll` unscoped = **10.6 MB / 5,704 rows / ~7 s** (worse after the clerkProc relation) vs `?month=YYYY-MM` = **199 KB / 0.23 s** (backend month filter existed all along, frontend never used it). `useCalendarSurgeriesQuery(month: Date)` now REQUIRES a month (key `['calendarManagerSurgeries','YYYY-MM']`, `placeholderData` keeps prev month during nav); create/delete mutations switched from per-key setQueryData to PREFIX invalidation; all 7 consumers pass their view month (5 calendar views' existing state — 3 needed the `useState` moved ABOVE the query call — + CM surgeries list gained a month navigator ‹ month › + Today, `prevMonth/nextMonth` i18n keys, `noSurgeries` reworded month-aware). Tradeoff: calendar grid spillover days from adjacent months won't show rows until navigated (standard).
**Procedure typeahead UI (2026-07-18, user ask, uncommitted)**: replaced the unstylable native `<datalist>` on `/dashboard/cm/surgeries/new` (rendered as a bare browser popup pinned to the viewport edge in RTL) with `src/components/PhraseCombobox.tsx` — custom WAI-ARIA combobox: anchored ds-card glass panel, live substring filter over phrase+translation with match highlighting, two-line bilingual options (phrase + other-language form, `dir="auto"` each), keyboard nav (arrows/Enter/Esc/Tab, aria-activedescendant, scrollIntoView), outside-tap close, `onMouseDown preventDefault` so option clicks don't blur-close, exact-match ✓ icon, no-matches state with "new phrase will be learned" hint (`newPhraseHint` i18n key EN+AR), 45vh max panel (mobile-safe), capped at 100 rendered options. Free text still always accepted (suggestions, not constraint).
**REVISED per user (2026-07-18): the CM surgeries LIST is a WORK QUEUE, not a month browser** — I had month-scoped it without consulting (user called it out; the clerk wants to instantly see what they just created, and a new surgery whose procDate is in another month would vanish from a month view). Now: new backend `?recent=<n>` mode on `/calSurg/getAll` (validator 1–200, controller short-circuit, `getRecentCalSurg` service — `updatedAt DESC`, bounded, clerkProc loaded) + frontend `api.getRecentCalendarSurgeries` / `useRecentCalendarSurgeriesQuery` (key `['calendarManagerSurgeries','recent',limit]` — same prefix, so create/delete invalidation refreshes it); CM surgeries page = recent-first 100, month navigator REMOVED (user chose "recent-first only"; month browsing = the calendar view), subtitle now "Latest created and edited procedures" EN+AR, prevMonth/nextMonth keys dropped. The 5 CALENDAR views stay month-scoped (inherently monthly). Verified live: `?recent=100` → 100 rows updatedAt-DESC (today's test surgery first), ~198 KB.
**Backend fix (same day): `/calSurg/getAll` family now loads `clerkProc`** — the CM calendar/table showed CPT titles instead of the clerk's phrase because only the `/calSurg/dashboard` method loaded the relation; `calSurg.service.ts` `relations:["hospital","procCpt"]` → `+"clerkProc"` at all 10 sites (getAll/byMonth/byDay/byYear/byDateRange/byId/create/update/search). Verified live: getAll emits clerkProc{title,titleAr,titleEn} on 4,734/5,704 rows (970 legitimately procedure-less). Frontend needed NO change (normalizer + display already prefer clerkProc). Also added **`nodemon.json` (watch src/ only)** — doc/CLAUDE.md edits were restarting the dev server mid-request, which killed background Gemini enrichment (the one NULL patientNameEn case) and caused ~17s cold-boot waits misread as slow saves.
**CM RTL/i18n polish pass (2026-07-18, ✅ COMMITTED: backend `0b0d946` on `migration/mysql-to-postgres`, frontend `a4af9b0` on `design-integration`; ⚠️ the `.env.staging` REF_API_TIMEOUT_MS 15000→30000 change is gitignored = THIS MACHINE ONLY)**: (1) **AR placeholder alignment** — `dir="auto"` on an EMPTY input resolves LTR (placeholder ignored), so all CM form inputs now use `dir={value.trim() ? 'auto' : undefined}` (surgery form patientName, PhraseCombobox, event form refSearch+location, CalSurgEditModal patientName); date/time inputs get `dashboard.css` rules `.app-shell[dir='rtl'] input[type=date|time|datetime-local] { direction: rtl }` + `::-webkit-datetime-edit { text-align: right }` (Chrome UA sheet pins these controls LTR and the inner shadow editor keeps locale alignment — BOTH rules needed). (2) **Lecture picker on event form**: backend legacy `GET /lecture` list now emits additive `lectureNumber`+`arTitle` (referenceRead.provider — by-id had them, list didn't); picker replaced native `<select>` with a **two-column listbox** (fixed-width mono LTR number cell | language-aware title, click-to-select, prefill scroll-into-view) because native options can't be styled and mixed digit+Latin numbers ("2.1.1b") font-fallback bigger than digit-only ones in AR; searchable by number + BOTH titles. Journals/confs keep the native select. (3) **Event lecture shape repaired app-wide**: raw event endpoints (getAll/byId/create/update via `populatePresenter`) emitted post-conform `lecture.title` while EVERY frontend consumer reads `lecture.lectureTitle` — silently broken titles since migration 610090; `populatePresenter` now aliases additively (`_id`, `lectureTitle`), the 2 hand-mapped shapes (presenter/dashboard) also carry arTitle+lectureNumber. (4) **Events list + CM calendar presentation**: shared `utils/eventTitle.ts` (`getEventTitleParts`/`getEventDisplayTitle`) + `components/EventTitle.tsx` — **alignment follows UI language, direction follows content** (cell keeps table direction, title span dir=auto isolated, number = LTR mono span with language-picked physical margin — logical `me-*` resolves against the SPAN's forced ltr, not the row!); calendar tiles show title WITHOUT number for ALL roles (user decision; number lives on list+form); Calendar.tsx gained opt-in `renderEventTitle` prop (EN-only pages untouched), localized type badges (محاضرة/مناقشة بحث علمي/مؤتمر — EN also fixed from raw "conf" to "Conference") + location (القسم/اونلاين, case-insensitive vs legacy "dept") with MapPin/Globe icons; new `calendar.*` i18n keys typeLecture/typeJournal/typeConf/locationDept/locationOnline. (5) **clerk_procs enrichment self-healing**: a phrase typed ONCE whose background hub resolve failed transiently (Railway hub cold start > 15s timeout; translation succeeded because Gemini runs later/warm) stayed NULL forever — added `ClerkProcService.retryUnresolved()` (≤5 rows, >2 min old, procCptId OR title-slot NULL, overlap-guarded) piggybacked on the 5-min RefData poll + a 30s post-boot sweep; `.env.staging` REF_API_TIMEOUT_MS 15000→30000. Verified live: the stuck row "شفط دم من المخ" self-healed to CRAN evacuation / cranial trauma / 0.736 via the boot sweep. NB deployed Railway hub is PRE-id-enhancement (returns no procCptId/mainDiag ids — spoke's exact mirror fallback by (alphaCode,numCode)/(dept,title) does the id work). tsc+build clean both repos.

### 🧹 DROP MULTI-TENANCY → DB-backed single institution — ALL STAGES DONE + VERIFIED (2026-07-17, uncommitted)
Design: `docs/DROP_MULTITENANCY_INSTITUTION_TABLE_PLAN.md` · Ledger (read FIRST): `docs/DROP_MULTITENANCY_PROGRESS.md`. User directive: the KA spoke serves exactly ONE institution forever — rip `institutionId` out of routing entirely, delete `DataSourceManager`, but keep the institution as a real DB row so feature flags survive. Approved decisions: D1-A (keep `req.institutionDataSource` name, source from the one `AppDataSource`), D3 (frontend fetches `GET /institution`), D5 (redirect legacy id-URLs).
- **Backend (branch `migration/mysql-to-postgres`):** new `institutions` table + 1 KA row (migration `1783782610150`, id `550e8400-…`, all flags true) + `InstitutionEntity`; `institution.service.ts` now DB-backed (`getInstitution()` cached, warmed at boot index.ts:122; legacy `getStaticInstitution`/`getInstitutionById`/etc. are thin wrappers, `IInstitution` dropped its per-tenant `database{}` block); **`config/datasource.manager.ts` DELETED**, `institutionResolver.middleware.ts` rewritten as a slim context-attacher (same export name → ~30 routers untouched; still sets `req.institution`/`institutionDataSource`/`institutionId` from the DB row); waSession/waBot/auth.router/index repointed to `AppDataSource`; **`institutionId` removed from JWT sign/refresh, 4 validators, auth.interface, auth.controller login+refresh flows** (old tokens still authorize — nothing rejects the stale claim; `departmentId` claim kept); new public `GET /institution` (routes.config.ts, apiRateLimiter, response under `.data`); `INSTITUTION_ID` dropped from required-env (now inert). Verified: authed `/calSurg/clerkProcs` returns real KA data via the new middleware, `/institution` serves flags, sign+decode drops institutionId/keeps departmentId, migration apply+revert clean.
- **Frontend (`F:\WebDev\NeuroLogBookFront`, `design-integration`):** F1 `api.getInstitution()` + App bootstrap fetch → DB-sourced flags (STATIC_INSTITUTION = fallback); F2 `availableInstitutions` array removed + 8 login/signup pages use single `selectedInstitution` (institution dropdowns gone); F4 `:institutionId` route param collapsed (49 segments) + 6 legacy redirects (`LegacyInstitutionRedirect`) + new `useInstitutionId()` hook replacing ~41 `useParams` reads + ~42 files' nav-strings stripped; F5 already done (logins send only `{email,password}`, User model never had institutionId). **F3 (X-Institution-Id headers in api.ts) SKIPPED per user** — inert, backend ignores it. tsc + vite build both clean. ⚠️ **Runtime SPA nav NOT click-tested from CLI — smoke-test i-admin/super-admin/cm dashboards.**
- **Gotcha (re)confirmed:** writing temp files into a repo makes its nodemon/vite watcher restart mid-request → HTTP 000 that looks like a crash but isn't; test authed backend flows on an alt-port ts-node instance, not the user's :3001.
- **Nothing committed.** Backend :3001 (nodemon) + frontend :3000 (vite) are the user's live dev servers.

### 🚀 Decoupling started: LibelusRefApi (hub-and-spoke) — SCAFFOLDED + VERIFIED (2026-07-10); repo pushed + CPT endpoints added (2026-07-11)
**Architecture decision (settled with user):** hub-and-spoke — **1 standalone Reference/Dataflow API ("hub") with its own repo/deployment**, and **one API + one database per institute ("spokes")** that pull reference data from the hub over HTTP. Rationale: institutes have niche behavioral requirements while the reference dataflow is identical; per-institute instances avoid `if (institution===X)` spaghetti and give per-customer release cadence/blast-radius. Discipline: ONE spoke codebase/build, niche = per-institute config/optional modules, never repo forks. Plan doc: `~/.claude/plans/so-the-current-sql-ethereal-pike.md`.
- **New standalone project `F:\WebDev\LibelusRefApi`**. Repo created by user 2026-07-11; initial commit pushed by the Claude agent working in that project. ⚠️ Remote = `git@github.com-personal:R-I-F/LibelusRefApi.git` (**personal** account, NOT the Medscribe org alias originally planned — flagged to user). Read-only serving layer; **migrations + embedding backfills STAY in MedScribeNeuroServer** (user decision) against the same staging `defaultdb` (`PSQL_*_DEFAULT` + `ca-defaultdb-staging.pem` copied into its gitignored `.env`).
- **Auth = `X-API-Key`** (env `API_KEYS`, comma-separated `label:key` per consumer; SHA-256 lookup; per-key rate limiting). No JWT, no institutionResolver, no tenant code.
- **Endpoints** (all `/v1/*` need the key): `/health`, `/v1/version` (dataVersion = `migrationCount.maxId` of the `migrations` table — NOT max timestamp, one legacy migration has an outlier `375…` ts), `/v1/departments` (+`/:deptCode/main-diags`, `/:deptCode/diagnoses`, `/:deptCode/proc-cpts`), `/v1/main-diags/:id/diagnoses`, `/v1/main-diags/:id/proc-cpts`, `/v1/refAdditionalQuestions/department/:deptCode` + `/main-diag/:id`, `/v1/refLectures/department/:deptCode?level=`, **REST `POST /v1/diagnosis-search`** (`{deptCode|departmentId, query, limit}` — new, search was MCP-only in the monolith), **REST `POST /v1/procedure-search`** (same body contract; pgvector cosine over `proc_cpts.embedding`, dept-scoped via `main_diag_procs→main_diags.departmentId` — procs have NO direct dept link table), `POST /mcp` (`search_diagnosis` + `search_procedure`, both accept `deptCode`).
- **CPT additions verified on staging 2026-07-11** (uncommitted in the LibelusRefApi working tree): dept proc-cpts counts exact (NS=93, VASC=115); procedure search NS brain-tumor→CRAN 61510 resection, UROL ureteric stone→STON 52353 ureteroscopy w/ lithotripsy, MCP CTS bypass→CABG 33510; 400/401/404 paths correct; diagnosis-search regression OK (`8B01`). `proc_cpts` embeddings were already complete (1434/1434, HNSW cosine index) — no backfill needed. New module `src/procedureSearch/` mirrors `src/diagnosisSearch/`.
- Stack mirrors the monolith idiom (Express 5 + Inversify + raw-SQL providers on a ported `ReferenceDataSource`; `EmbeddingClient` = the `embedText` half of `AiAgentService`, same `gemini-embedding-001`/768).
- **Verified end-to-end on staging 2026-07-10**: tsc clean; 401 without/bad key; 15 departments; CTS questions = 6 keys; NS lectures msc=84; cns-tumors category reads (30 dx / 11 procs); REST search "bleeding inside the brain of an elderly patient after minor trauma" → `8B02` chronic subdural hematoma (correct); MCP initialize + tools/call → `8B01` SAH for "sudden severe headache with neck stiffness" (correct); `/v1/version` → `217.279` / `AuthorOphthalLecturesPart2…0217`.
- ⚠️ **Observed staging-data drift vs older notes here** (verified directly in DB, API is faithful): NS `department_diagnoses` = **124** (older note said 134); NS lecture levels now **md=66 / msc=84 / NULL=2** (the 189-era note said md=62/NULL=6 — 4 NULL rows have since been given `md`).
- **Next steps:** ~~user creates the dedicated GitHub repo + first commit~~ ✅ done 2026-07-11 (but on the personal account — confirm intended); commit/push the CPT additions from the LibelusRefApi tree (coordinate with the agent session working in that repo); institute-backend (spoke) HTTP client + caching keyed on `dataVersion`; later phases per plan (independent reference DB, production cutover).

### 🚧 KA-spoke conversion — plan approved, Stage A DONE (2026-07-11)
Full staged plan: `docs/KA_SPOKE_CONVERSION_PLAN.md` (gitignored) — converts THIS repo into the KA institute's single-institution spoke: reference modules removed, reference reads via mirror tables synced from the hub (pattern debated & settled: mirror + hub-governed re-mirror broadcast; snapshot rejected — volatile codes), multi-tenant machinery pinned to a static KA institution, new dedicated staging DB. **Hard guardrails**: never touch `main`; never write to production DBs (`SQL_DB_DEF_NAME_*`, read-only SELECTs OK); staging services only; `.env` never edited (only `.env.staging`).
- **Stage 0** ✅ mostly done: dedicated staging Aiven service `ka-psql-logbooknative.j.aivencloud.com` (PG 17.10) + DB `ka-institute` created, connectivity verified (same per-project CA `ca-defaultdb-staging.pem`), creds in `.env.staging` as `PSQL_*` (no `_DEFAULT`). Remaining: production read-only captures (KA institutions row + NS six-flag map), `pre-spoke-split` tags.
- **Stage A** ✅ DONE (hub repo, uncommitted): reference pipeline (217 migrations + staging-migrations.config + run/revert + both backfills rewired to `EmbeddingClient`) moved into LibelusRefApi, npm scripts added there; **`db:migrate:staging` MUST now be run from LibelusRefApi, not here** (local copies here remain until Stage E). New hub superadmin re-mirror broadcast: `POST /v1/admin/resync-broadcast` (X-Admin-Key) + `resync:broadcast` CLI + `SPOKE_WEBHOOKS` registry. All verified (no-pending, backfills no-op, version 217.279 unchanged, auth paths).
- **Stage B** ✅ DONE (uncommitted): Postgres-compat entity pass (75 substitutions: datetime→timestamp, tinyint→smallint, charset/collation stripped; **14 FK shadow columns char(36)→uuid** — FK columns shadowing @JoinColumn to uuid PKs; whatsappSession longtext→text + unsigned dropped; lectures.google_uid & level now nullable for hub mirroring); `src/department/` deleted (unused). New `src/config/ka-migrations.config.ts` (26-entity union, `PSQL_*` env) + git-tracked `src/migrations-ka/` (`InitKaSchema` generated — 30 tables/22 FKs/19 enums + uuid-ossp; `SeedKaLookups` — positions/approaches/regions + `ref_sync_state`) + npm `db:ka:migrate|revert|generate`. Verified on `ka-institute`: apply/idempotent/revert-cycle clean, submissions FKs RESTRICT, seeds 5/15/4, tsc clean. ⚠️ Tenant entity metadata now Postgres-native — the 1735* MySQL migrations are incompatible with these entities (expected; they never ran on PG).
- **Stages C+D+E** ✅ IMPLEMENTED by OpenClaw, committed `0087757` on the branch: static-institution shims (env-backed `getStaticInstitution()`, pinned DataSourceManager, resolver defaults-to-static, institutionId optional in auth validators), `src/refApi/` (client/refData poll/mirror sync/HMAC resync webhook at `/admin/ref-resync`) + `src/referenceRead/` serving legacy GET paths **from the mirror tables** (plan's sanctioned fallback variant — reads and FKs share one source), deletions per plan (mcp/ref*/diagnosisSearch/referenceDb/defaultdb configs, legacy routers+controllers of mainDiag/diagnosis/procCpt/lecture/positions/approaches/regions, `src/migrations/`, embedText); mysql2 + legacy gitignored `scripts/` deliberately retained.
- **REVISED (my independent review, 2026-07-11 evening)** — all green: mirror↔hub parity EXACT (10/124/93/134/153/152), boot+poll, auth without institutionId, removed routes 404, webhook HMAC both ways, `/references` bundle, tsc. INSTITUTION_ID `550e8400-…` + code `cairo-university` = canonical values from `scripts/seed-institutions.ts` (NOT fabricated; staging defaultdb never even had an institutions table). **Six-flag gap closed by me**: production truth captured read-only from MySQL kasr-el-ainy (14 nonzero flags — matches hub migration 158's capture), seeded via `src/migrations-ka/1783782609910-SeedKaSixFlags.ts` (title-keyed; applied ✅; uncommitted). **Hub broadcast wired end-to-end**: SPOKE_WEBHOOKS set in hub `.env` (localhost:3001 — update on deploy); `resync:broadcast` → KA 200 + sync report.
- **Still open:** deep tenant E2E (hospital→calSurg→POST /sub→analytics→PDF) with fixtures; frontend usage audit; commit SeedKaSixFlags here + Stage-A/SPOKE_WEBHOOKS work in the hub repo (on explicit ask).

### 🚧 Mirror WIDENED to all 15 departments (OpenClaw, 2026-07-12) + my revision — VERIFIED, dept-scoping added (uncommitted)
KA spoke now mirrors the hub's FULL structure (one tenant = Kasr Al Ainy, 15 departments): new mirror tables `departments` (hub UUID PK; no isActive — only active depts are served/mirrored) + `lecture_topics` (deptId FK); `main_diags.departmentId` FK; `department_diagnoses` M2M; `lectures` gained `topicId`/`lectureNumber`/`sortOrder`/`arTitle` (mainTopic kept = topic title for legacy shape); users (candidates/supervisors/institute_admins/clerks) gained nullable `departmentId` FK (dormant until frontend sends it); text-widening migration for multi-dept titles. Migrations `1783782609920-AddDepartmentScoping` + `930-WidenMirrorTextColumns` (applied ✅). Mirror sync loops all departments from `/v1/departments`, dedupes shared rows, chunks batched upserts.
- **My revision verdict: structurally faithful, count-EXACT vs hub** (15 depts, 196 main_diags, 1319 dx, 1529 department_diagnoses, 1643 mdd, 2075 mdp, 141 topics, 3237 lectures — lectures/topics equal the sum of all authored curricula). proc_cpts 1429 vs hub 1434: the 5 missing are hub rows linked to NO category (unreachable via API — hub data-quality note, not a spoke bug). ~~Deliberate projection: mirror drops Arabic on main_diags/diagnoses/proc_cpts~~ → **REVERSED 2026-07-15 (user directive — "honest table mirror")**: that projection silently discarded six hub fields that were 100% populated; fixed by migration `1783782610100-HonestMirrorAllHubFields` (+ mapper/sync carry all fields) + forced full resync — verified proc_cpts 1436/1436 arTitle+arDescription, diagnoses 1319/1319 icdArName+description+arDescription, main_diags 196/196 arTitle, values hub-identical (فتح التأمور, نزيف تحت العنكبوتية). **STANDING RULE: the mirror carries EVERY hub field — no projection; choices that discard data get ASKED first, never footnoted.** (procCpts now 1436 — hub migration 234 added 7 for the calSurg mapping.)
- **Defect found + FIXED by me: reads had NO department filter** — `/mainDiag` etc. would dump all 196 categories into the NS frontend. Added `src/referenceRead/referenceRead.provider.ts` (dept-scoped queries: main_diags by departmentId; diagnoses via department_diagnoses; procs via main_diag_procs→main_diags; lectures via lecture_topics) + `?deptCode=` query param on the 4 list routes (case-insensitive, validated, unknown→404, malformed→400), **default = `REF_DEPT_CODE` (NS)** so the pre-multi-dept frontend behaves byte-identically. Verified live: default→10 NS (aq attached), UROL→13, ophthal→12, ZZ→404, `1!`→400; SQL-level NS 124/93/152, CTS 99/103/625, GS 96/100/493.
- ⚠️ **5 main_diag titles now duplicate across depts** (head & neck cancer, pancreatic cancer, appendicitis, hepatocellular carcinoma, cleft lip & palate) — none NS, so SeedKaSixFlags stays correct, but **future title-keyed seeds must also match departmentId**.
- ~~Open: users.departmentId not yet set by registration~~ → ✅ **ACTIVATED at the API layer (2026-07-12, uncommitted)**: `registerCand`/`registerSupervisor` accept optional `departmentId` (UUID, validated against the mirror `departments` table — unknown id rejected); login (candidate/supervisor, instituteAdmin, clerk) embeds a `departmentId` JWT claim when the user has one (required adding the field to `AuthTokenService.sign/signRefreshToken` — they whitelist payload fields; refresh preserves it); referenceRead department resolution is now **`?deptCode` → JWT `departmentId` claim (skipped silently if stale) → `REF_DEPT_CODE`**; NEW public `GET /departments` (apiRateLimiter, no JWT — like /institutions) lists the 15 mirrored departments for signup pickers. Verified live: UROL-registered candidate's JWT carries the claim and `/mainDiag` returns the 13 UROL categories with no param; `?deptCode=NS` overrides; legacy users (no departmentId) unchanged (NS default). Frontend TODO: send `departmentId` at registration + department picker from `GET /departments`.
- ✅ **Hub repo committed + pushed** (`731430d` on LibelusRefApi `main`): Stage-A pipeline move + admin resync broadcast.
- **`/audit-module <NAME>` skill added (2026-07-12)** (`.claude/skills/audit-module/`, gitignored local tooling): ANALYSIS-ONLY per-module upgrade audit — compares old production pattern (`main` code read via `git show`, NEVER checkout + production MySQL `SQL_DB_DEF_NAME_KA` **READ-ONLY**) vs the new single-tenant KA spoke (branch + `ka-institute` PG), then writes an upgrade plan to `docs/audit_module/AUDIT_MODULE_<NAME>.md` for approval. It never codes/migrates/writes to any DB. DB access only via its `query.js` (3 guard layers, all verified live: statement whitelist SELECT/SHOW/DESCRIBE/EXPLAIN, forbidden-verb scan, server-side read-only session — MySQL `@@transaction_read_only=1` confirmed, PG blocks UPDATE). Targets: `prod` (kasr-el-ainy), `prod-cts` (`SQL_DB_DEF_NAME_KA_CTS` clone), `ka` (staging PG). Gotcha handled: `.env`/`.env.staging` use `KEY: value` colon syntax + `.env` is CRLF (JS regex `.` doesn't match `\r`) — query.js parses both. Module resolution consults `docs/module list.md`.
- ✅ **instituteAdmin module audited + upgraded (OpenClaw 2026-07-12, revised by me 2026-07-13, committed)**: audit doc `docs/audit_module/AUDIT_MODULE_instituteAdmin.md` + new `docs/module list.md` (full src/ module inventory). Smallest module — only the entity differed main→branch; the 96 KB dashboard provider is dialect-safe QueryBuilder except ONE hand-written SQL: `getInstituteAdminByEmail` used MySQL `SUBSTRING_INDEX` → ported to PG `split_part` (same pattern as cand `51a02d0` / supervisor `4998433`; this method is called by passwordReset, so admin forgot-password was silently broken on PG). ETL via gitignored `scripts/etl-institute-admins-prod-to-ka.cjs`: 3 prod admins → `ka-institute`, **all NS-scoped (user choice)**; prod-cts test account excluded; idempotent ON CONFLICT upsert. **Key decision: admin `departmentId` stays NULLABLE** (department-scoped by default; NULL = future institute-scoped special admin) — deliberately opposite of cand/supervisor NOT NULL; `POST /instituteAdmin` keeps it optional; NO migration needed (enum already superset, no phoneNum-unique in prod). My re-verification: tsc clean; live ka DB 3 rows / 0 NULL dept / all 3 joined to NS; ETL script write-path is PG-only. Out of scope (later task): per-department dashboard read-filtering for scoped admins.
- ✅ **superAdmin module audited + implemented (2026-07-13, user-approved; doc uncommitted)**: audit doc `docs/audit_module/AUDIT_MODULE_superAdmin.md`. **Cleanest module of the series — zero code changes, zero migrations**: provider has NO raw SQL (no SUBSTRING_INDEX bug), auth login already spoke-converted, live ka schema index-exact vs prod, `super_admins` deliberately has NO departmentId/claim (institution-global role). Only gap = ETL of the **single prod super admin (the owner account)** via gitignored `scripts/etl-super-admins-prod-to-ka.cjs`: id + **bcrypt hash byte-verbatim** (critical — superAdmins are excluded from forgot-password), prod-cts not read (verified identical copy of the same row). Verified: all ETL checks ✅ + staging login smoke test (wrong password on real account → "Invalid credentials" = row found + bcrypt ran; unknown email → "not found"). **Findings during implementation**: (1) `/auth/superAdmin/login` is `NODE_ENV=development`-gated (`auth.router.ts:193` — IDENTICAL on main, production parity; staging 403 is expected; smoke test used a runtime NODE_ENV override, `.env.staging` untouched) — the superAdmin account is a dev-tool by design; with `POST /superAdmin` also disabled, DB-direct/ETL is the only provisioning path. (2) Deprecated `adminLogin` controller method is unrouted dead code (no `/auth/admin/login` route on either side).
- 🔧 **Lecture-conform breakage FIXED (2026-07-14, my review session, uncommitted)**: OpenClaw's `088e8b2` ("conform to hub scaled schema": drop google_uid/mainTopic, rename lectureTitle→title, migration 610090) had CORRECT hub-pure data (fingerprint-verified: NS msc=84/md=66, 0 missing Arabic — NOT old-SQL rows, user's fear unfounded at data level) but shipped **broken serving**: it missed `src/referenceRead/` entirely → `GET /lecture` 500'd (raw SQL selecting dropped columns — tsc-invisible) and `/lecture/:id` returned the new shape. Fixed by aliasing (`l."title" AS "lectureTitle"`, topic title AS `mainTopic`) + new legacy-shaped by-id read; also fixed missed twin `.lectureTitle` at `event.service.ts:288`; **relinked all 81 events→lectures** dropped by the events ETL ("no crosswalk" was wrong — hub NS lectures ARE prod's, matched by lectureNumber 78 + title 3, 0 unmatched, full 81-row prod-consistency check) via `scripts/relink-event-lectures.cjs`. Verified live: /lecture NS=152/CTS=625 legacy shape, by-id with Arabic, event dashboard titles. ⚠️ **Zombie ts-node server (started 14:02, pre-migration-610090 metadata) was squatting port 3001** throwing `LectureEntity.lectureTitle does not exist` — killed; ALWAYS netstat :3001 before verifying. Full fix record: `docs/audit_module/AUDIT_MODULE_lecture.md`.
- ✅ **Institution-select legacy REMOVED both sides (2026-07-14, user-directed; uncommitted)**: **Backend** (this repo, branch): public `GET /institutions` retired — `institution.router.ts`+`institution.controller.ts` deleted, mount + DI bindings removed, `getStaticInstitution()`/interface kept (auth+resolver use it); verified live: `/institutions`→404, login WITHOUT institutionId passes validation (401 domain error, not 400), tsc clean. **Frontend** (`F:\WebDev\NeuroLogBookFront`, `design-integration` — NOT MedScribeNeuroFront, that repo has no such branch): `STATIC_INSTITUTION` const pinned in `authSlice` (id `550e8400-…` = INSTITUTION_ID, all three flags true — ~30 pages gate on `selectedInstitution.isAcademic/isPractical/isClinical`, so it must stay populated; logout resets to it, not null), `availableInstitutions=[static]` (login/signup pages auto-select single option), `InstitutionSelector.tsx` deleted, landing Log-in → `navigate('/login')` directly, `api.getInstitutions()` deleted, App bootstrap fetch + JWT-restore institution block removed, 4 login methods send `{email,password}` only; `tsc && vite build` clean. **Deliberate residue**: signup pages' one-option institution dropdown (replaced later by the department picker) + inert `X-Institution-Id` headers (~100 API methods, backend ignores). User's vite dev server (port 3000) hot-serves the new code; backend VITE_API_URL=3001.
- ✅ **calSurg proc-mapping APPROVED + calendar goes bilingual (2026-07-14, uncommitted)**: user approved the arab_procs→proc_cpts clinical mapping sheet with no gate ("no gate consider it approved") — sheet header stamped, 4,608/5,578 cal_surgs carry `procCptId` (970 have no procedure at all). **Backend**: `CalSurgEntity` gained the `procCpt` ManyToOne relation on the existing FK column; `/calSurg/dashboard` response now includes additive `procCpt {_id,title,numCode,alphaCode}` alongside `arabProc`. **Frontend** (`design-integration`): `CalendarProcedure.procCpt` type + normalizer mapping + `Calendar.tsx` `procTitle()` — EN mode shows the CPT title, AR mode the Arabic name, cross-language fallback, `dir="auto"` on patient/procedure lines. Verified live: dashboard 435 rows ALL with both titles (sample: غضروف ↔ fenestration 63042-00); both builds clean. Later: retire arabProc write-surface (table remains the AR-name source — mirror proc_cpts have no arName).
- ✅ **arab_procs RETIRED (2026-07-15, user: "retire it"; uncommitted)**: migration `1783782610110-RetireArabProcs` — dropped `cal_surgs.arabProcId` (+FK) and the `arab_procs` table; ~~preserved `proc_aliases` for the sheet-import~~ → **DROPPED too (user: "break it i dont care about that now"; migration `1783782610120`)** — the sheet-import now matches ONLY exact proc_cpts `title`/`arTitle`; colloquial sheet values ("غضروف") import with procCptId NULL. The colloquial→CPT pairs remain recorded in `docs/CALSURG_PROC_MAPPING_REVIEW.md` if ever needed again. **Backend sweep**: arabProc module+5 validators deleted, `/arabProc` unmounted; calSurg create/update/dashboard/filters now procCpt (`procedure` body key = procCptId; filter params renamed `procTitle`/`procNumCode`); sub relations `calSurg.procCpt` + mapper emits `procCpt {title,arTitle,…}` (frontend key changed arabProc→procCpt); PDF/email/aiAgent/reports/instituteAdmin hospital-analysis read procCpt (Arabic slot = arTitle||title); `/instituteAdmin/arabicProcedures` route removed; `GET /procCpt` role gate widened to **instituteAdmin+clerk** (replaces the retired clerk-accessible /arabProc picker) and its SELECT gained `arTitle`; external `/external` sheet-proxy renamed getSheetData (was misnamed getArabProcData); `IExternalRow` moved to `src/types/`. **Frontend (design-integration)**: ArabProc type/api methods/queries/2 admin pages/routes/nav/pageTitles deleted; calendar+forms+submission views read procCpt (AR=arTitle); create/edit pickers list proc_cpts (search matches EN+AR+codes). Verified live: /arabProc 404, dashboard 427 rows procCpt{title,arTitle} no arabProc key, clerk GET /procCpt 200 with arTitle; aliases 73; both builds clean. NB: 970 cal_surgs legitimately have no procedure (never did).
- ✅ **CLERK-PROCS LEARNING PIPELINE — ALL STAGES IMPLEMENTED (2026-07-15, uncommitted)**: full living record = `docs/CLERK_PROCS_LEARNING_PIPELINE_PLAN.md` (checkpoint table per stage — READ IT FIRST) + divergence report `docs/CLERK_PROCS_DIVERGENCE_REPORT.md`. Summary: `clerk_procs` learns clerks' free-text procedure phrases (one hub semantic search per new phrase/dept, repeats free); cal_surgs gained clerkProcId + bilingual patientNameAr/En (Gemini translit); staging wiped + rebuilt from prod READ-ONLY (5,578 cal_surgs / 3,599 subs / 5,948 keyed answers / 81 clerk_procs — approved mapping stored, algorithm agree-rate 44/73); POST /calSurg takes `procedureText`; GET /calSurg/clerkProcs = typeahead; calendar shows clerk's words (AR) / CPT (EN) + ALPHA. **Full prod snapshot (restore-tested) at `F:\DB_BACKUPS\` BEFORE any work.** Hub procedure-search id-enhancement implemented but uncommitted (spoke has exact mirror fallback). E2E verified live incl. learn-once dedupe. Follow-ups in the plan's ▶ Next action.
- ✅ **Bilingual patient-name display convention — ALL surfaces (2026-07-16, uncommitted)**: calendar showed raw (Arabic) `patientName` in EN mode. Frontend (design-integration): `CalendarProcedure`+`SubmissionCalSurg` types carry `patientNameAr/En`; `normalize.ts` passes them through (was silently dropping — the actual bug); `Calendar.tsx` `patientDisplayName()` (AR→ar slot, EN→en slot, fallback raw) in all 3 render spots; new `englishPatientName()` util (`utils/patientName.ts`) applied on the EN-labeled pages: CalendarManagerSurgeriesPage, Supervisor/AcademicPractical submission-form summaries, SubmissionDetailView, Supervisor(My)SubmissionsPage, AcademicPracticalSubmissionsPage; supervisorSlice derive carries the slots. Backend: `sub.mapper` emits `patientNameAr/En` on both calSurg shapes; **PATCH /calSurg name edits now re-run `PatientNameService.bilingual`** (provider + service passthrough) so slots never go stale (AI failure → other slot NULL, not stale). Both builds clean. Staging data 5,578/5,578 both slots. **Deliberately raw**: edit/create form inputs (they edit the source field), PDF/email templates + activity timeline (official record / server-built — flagged as plan Q5).
- 📋 **NEW PLAN v2 awaiting review: `docs/CLERK_PROCS_BILINGUAL_TITLES_AND_DELTA_SYNC_PLAN.md` (2026-07-16, revised same day from the user's edited requirement)**: Part 1 = clerk_procs gains `titleAr/titleEn` (Gemini TRANSLATION once per new phrase — token economics guarded; 81-row backfill covers ALL history, no cal_surgs remap; calendar EN mode shows the translated clerk phrase instead of CPT title, ALPHA suffix kept). **Dept-first-then-semantic narrowing = user DON'T-SKIP rule — VERIFIED already true in the hub** (`procedureSearch.service.ts` joins `main_diags.departmentId=$2` BEFORE the cosine ORDER BY; plan adds a permanent cross-dept leak test). Part 2 (NEW in v2) = **initials-aware name transliteration**: stored privacy format `أحمد م خ` sometimes fuses to "Ahmed Mkh" — fix = tokenize, Gemini ONLY for word tokens, single-letter initials via a deterministic AR↔EN digraph table (`خ→Kh`), + ~zero-token remediation script for existing 5,578 rows. Part 3 = pre-deployment **incremental** prod→ka delta sync (no wipe: id+`updatedAt` watermarks — verified prod cal_surgs+submissions both have `updatedAt ON UPDATE`; NEW/CHANGED/DELETED classification, dry-run default, FK-parent closure, subs+keyed-answers via the shared six-flag mapper, deletions report-only). Non-negotiable rules restated in the plan (prod MySQL read-only; ka-institute only write target; side branches only, never main). **v2.1 directive**: ALL source cal_surgs are NS-scoped + assigned to the single clerk `45eb7fb8-b9af-4bdd-90c8-8519cb4ce472` (Mohamed Ismail — verified only ka clerk; all 5,578 already NS; NO uploader field exists in prod/sheet so the "unless someone else" escape is an evidence check in the import report) → new `cal_surgs.clerkId` FK + full backfill + JWT stamping on create (plan §4.4). **v2.2: 7/9 questions ANSWERED by user 2026-07-16** — Q1 literal translation · Q2 CPT fallback · Q4 events out of scope · **Q5 OVERRIDE: bilingual standard at ALL levels incl. PDFs/emails → Stage D2 (EN+AR name lines like the Hospital pattern; timeline language-aware)** · Q7 digraph table delegated · Q8 server-side privacy-format enforcement yes · Q9 re-attribute 81 legacy clerk_procs yes. **v2.3: ALL 9 answered** — Q3 deletions OUT OF SCOPE ("nothing will be deleted from the source, I guarantee" → sync has NO delete mode, anomaly report only) · Q6 instant save YES (§3.6). **✅ ALL STAGES A→G IMPLEMENTED + VERIFIED 2026-07-16 (uncommitted)** — read the plan's checkpoint table FIRST. Summary: migration `1783782610140` (titleAr/titleEn + cal_surgs.clerkId FK backfilled 5,578 + 81 clerk_procs re-attributed + prod_sync_state); instant save (POST /calSurg **1.5 s**, was 16–20 s; `setImmediate` background = hub resolve + all Gemini; PATCH same); initials-aware transliteration (digraph tables in `patientName.service.ts`, synced copies in 2 .cjs scripts) + server-side privacy format in `UtilService`; **1,279 fused-initials rows remediated** (`fix-patient-name-initials.cjs`, idempotent); 81 phrases translated literal-register (`backfill-clerk-proc-titles.cjs`); calendar/manager/typeahead show clerk phrase per language; PDFs/emails got EN+AR name+procedure lines (sub relations now load `calSurg.clerkProc`); timeline language-aware. **Delta sync `delta-sync-prod-to-ka.cjs`** (dry-run default, NO delete mode, per-row updatedAt compare, parent-ETL closure, learning pipeline for new phrases): applied 2026-07-16 after fresh restore-tested snapshot `F:\DB_BACKUPS\kasr-el-ainy-20260716.sql.gz` → **+125 cal_surgs +17 subs from prod (now 5,703/3,616 = prod parity)**, all 29 delta phrases reused free, invariants green (dept=1 NS, clerkId NULL=0, slotless=0), second dry-run 0/0/0 = idempotent; rerunnable anytime + final run before cutover. E2E: "أحمد م خ"→"Ahmed M Kh" (exact user example), learn-once, dept-leak test clean. NOTHING committed.
- ✅ **Department-driven narrowing — future multi-dept (2026-07-16, uncommitted)**: procedure-search narrowing now follows the clerk's assigned department instead of always NS. Order (create + typeahead): **body/`?deptCode` → JWT `departmentId` claim → REF_DEPT_CODE (NS)**. Backend: `calSurg.controller` create resolves `body.departmentId ?? jwt.departmentId`; `getClerkProcs(ds, jwtDepartmentId?, deptCode?)` same order (clerk login already signs the dept claim, `auth.controller.ts:493`). Frontend (design-integration): `User.departmentId` threaded through clerk login + auth-restore (from token payload); new `Department` type + `api.getDepartments()` + `useDepartmentsQuery`; `PostCalSurgBody.departmentId?`; **surgery-create form shows a REQUIRED Department picker only when `!user.departmentId`** (un-scoped user) — scoped clerks narrow via the JWT claim, no picker. No disruption today (sole clerk = NS-assigned `65bda505…` → resolves to NS = old default). Verified live: JWT dept=ORTHO→ORTHO, body dept=UROL→UROL, none→NS. Both builds clean. ⚠️ test-cleanup gotcha: background enrich `setImmediate` can resurrect a clerk_procs row deleted concurrently (delete after creates settle; irrelevant in prod).
- **DECISION (user, 2026-07-12) — additional questions for non-NS departments**: do NOT author six-flag values per department and do NOT copy production; the per-department question config comes from **the hub's scaled additional-questions framework** (98 questions / 472 options / 700 links / 937 narrowing rows, migrations 157–187 in defaultdb), **probably mirrored** into the spoke like the rest of the reference data (4 tables: additional_questions[scaled]/question_options/main_diag_questions/main_diag_question_options — note the spoke's legacy six-flag table is also named `additional_questions`, so the mirror will need renamed local tables or the legacy one retired). Six-flag stays NS-only legacy until that cutover (frontend change required). NOT implemented yet.

All on **staging**. Migrations `1750000000001`–`1750000000196` applied. **MFS 111–116 committed** (`30829e6`); SOC 104–110 (`20f2f8a`); **TRS 117–123 + OBGYN 124–131 committed** (`3bcdcd5`); **ENT 132–138 committed** (`c4e0b37`); **OPHTHAL 139–144 committed**; **UROL 145–151 committed** (`73674d0`); **ampersand-cleanup 152–155 committed** (pushed to `migration/mysql-to-postgres`). All migration files force-added to git.

### ✅ Ampersand ICD-code cleanup — COMPLETE (migrations 152–155)
Cross-department data-quality pass (post-audit). Goal: every `diagnoses.icdCode` carries a single unique ICD-11 stem/leaf — remove all post-coordination ("&" cluster) codes. **CPT/`proc_cpts` never use "&" (0 affected — "&" is ICD-11-only syntax).** 53 diagnosis rows had "&" codes.
- **152** RemoveAmpersandIcdCodes: 39 KEEP (rename to nearest free single leaf, embedding reset), 14 MERGE (delete &-row that duplicates an existing same-dept plain row), + TRS top-up (acute/chronic transplant rejection → `NE84`; immunologic-rejection category restored to ≥5 via acute & chronic GVHD `4B24.0`/`4B24.1`; sigmoid-volvulus merge relinks shared GS `DB30.1` → PEDSURG malrotation category). All resolutions WHO-verified via `icd11_search`.
- **153** TopUpAfterAmpersandCleanup: the 14 merges dropped VASC/HBP→96, PEDSURG→99, TRS immunologic-rejection→3; restored all to the ≥100 floor & category ≥5 with distinct ICD-11-verified diagnoses.
- **154** TopUpDeptsTo100: +distinct verified diagnoses to VASC/HBP/PEDSURG (posterior urethral valve shared UROL→PEDSURG).
- **155** TopUpVascHbpFinal: +1 each VASC/HBP after two codes chosen in 154 turned out to already exist.

**State after 155 (verified on staging 2026-06-28): 0 diagnoses with "&" in `icdCode`, 0 proc_cpts with "&" in `numCode`/`alphaCode`, 0 NULL embeddings on either table. All 15 dept audits remain complete; every dept ≥100 diagnoses.**

### ✅ Scaled additional-questions framework — SHIPPED (migrations 157–158, uncommitted)
Full design: `docs/ADDITIONAL_QUESTIONS_SCALED_FRAMEWORK_PLAN.md` (gitignored). Replaces the legacy per-tenant six-flag model (`spOrCran/pos/approach/region/clinPres/intEvents` + tenant-global positions/approaches/regions) with a generic per-department per-mainDiag framework in **defaultdb**: **questions are rows, not columns.**
- **157** CreateAdditionalQuestionsFramework: 4 tables — `additional_questions` (per-dept defs: `key` camelCase, bilingual label, `inputType` single_choice|multi_choice|free_text, UNIQUE(dept,key)), `question_options` (bilingual, UNIQUE(question,value)), `main_diag_questions` (replaces the six flags; optional isRequired/sortOrder overrides), `main_diag_question_options` (optional per-mainDiag option narrowing; **no rows = all options apply**). Integrity rule (app-level): question.departmentId must equal main_diag.departmentId.
- **158** SeedNsCtsAdditionalQuestions: seeded from production truth captured 2026-07-08 (title-matched 1:1). Canonical keys: `surgicalDomain` (generalized spOrCran "Or" question), `position`, `approach`, `region`, `clinicalPresentation`, `intraopEvents`. NS = 6 questions / 26 options / 14 links; CTS = 4 questions (no surgicalDomain/clinPres — all-zero in prod) / 21 options / 63 links; CTS `region` labelled "Target structure". Question `arLabel` filled. **161** backfilled Arabic `arValue` on all 65 options (0 NULLs); from 161 on, option inserts MUST include `arValue` (skill enforces it; query.js flags `⚠️ NO_AR`).
- **New module `src/refAdditionalQuestions/`** (provider/service/controller/router + 2 validators): raw-SQL reads on `ReferenceDataSource` (no entities, no institutionResolver). Routes (JWT, all 4 roles): `GET /refAdditionalQuestions/department/:deptCode`, `GET /refAdditionalQuestions/main-diag/:mainDiagId`. Bound in `container.config.ts`, mounted in `routes.config.ts`. Legacy tenant module `src/additionalQuestions/` untouched (production uses it until cutover).
- **Verified on staging**: counts exact, 0 integrity violations, revert→re-apply cycle clean, provider smoke test (`scripts/smoke-ref-additional-questions.ts`) green. Typecheck clean.
- **Deferred**: `additionalAnswers` JSONB on tenant submissions (keyed by question `key`); question/option content for the other 13 depts (author dept-by-dept via `/audit-questions <DEPT>`); admin write API.
- **`/audit-questions <DEPT>` skill added** (`.claude/skills/audit-questions/`): professor-persona review/authoring of a dept's question config (critique → question set → options → link matrix → narrowing → migration → apply → verify). Resumable via `MEDICAL_CODE_AUDITS/<DEPT>/QUESTIONS_<DEPT>.md`.
- **CTS professor review — COMPLETE (migrations 159–160, applied ✅)**: save-game `MEDICAL_CODE_AUDITS/CTS/QUESTIONS_CTS.md`. +`urgency` (all 17 categories) + `cpbStrategy` (10 cardiac categories); approach +4 options (right posterolateral thoracotomy, left anterolateral thoracotomy/MIDCAB, mediastinoscopy, other), region +4 (chest wall — its own category had NO valid target!, trachea / airway, atria / appendage, whole heart), position +other; position added to congenital acyanotic (PDA/coarctation via thoracotomy); region UNLINKED from 8 single-target categories (non-discriminating); intraopEvents sortOrder→9 (renders last); **99 narrowing rows** (74 approach + 25 region) — first use of `main_diag_question_options`. **CTS now: 6 questions / 39 options / 83 links / 99 narrowing.** surgicalDomain deliberately skipped for CTS (axis duplicates the categories). NS still the untouched production mirror (6q/14 links).
- **GS questions authored — COMPLETE (migrations 162–163, applied ✅ 2026-07-09)**: save-game `MEDICAL_CODE_AUDITS/GS/QUESTIONS_GS.md`. Fresh authoring: 6 questions (approach/region/urgency + NEW GS keys `woundClass` [CDC wound classification] + `stomaFormed` + intraopEvents) / 34 options (all with Arabic) / 56 links / 65 narrowing rows. region only where the diagnosis doesn't already encode the site (breast side, thyroid lobe, colorectal segment); woundClass dropped where single-answer (breast/thyroid clean, bariatric uniformly clean-contaminated); approach not on breast/thyroid (approach = procedure there). **GS: 6q/34opt/56 links/65 narrowing.**
- **ORTHO questions authored — COMPLETE (migrations 164–165, applied ✅ 2026-07-09)**: save-game `MEDICAL_CODE_AUDITS/ORTHO/QUESTIONS_ORTHO.md`. Fresh authoring: 8 questions — NEW ORTHO keys `laterality` (14 non-axial categories; reusable for other depts), `fixationMethod` (the fracture viva; 11 options cast→arthroplasty), `fractureType` (closed/open + Gustilo grades) + approach (open/arthroscopic/mini-open/percutaneous/MIS — NOT on fracture cats, redundant with fixation)/region ("Bone / joint involved", only tumours/osteomyelitis/osteonecrosis)/position (only where it varies: beach chair/traction table/THR)/urgency (only the 8 cats with real urgent share)/intraopEvents. 47 options (all Arabic) / 65 links / **102 narrowing rows** (approach 42, fixation 37, region 13, position 10; osteomyelitis region deliberately un-narrowed). **ORTHO: 8q/47opt/65 links/102 narrowing.**
- **PRS questions authored — COMPLETE (migrations 166–167, applied ✅ 2026-07-09)**: save-game `MEDICAL_CODE_AUDITS/PRS/QUESTIONS_PRS.md`. Fresh authoring: 5 questions — NEW PRS key `reconstructionMethod` (the reconstructive ladder: primary closure→secondary intention→sTSG/FTSG→local/pedicled/free flap→tissue expansion; 9 categories) + laterality (reused ORTHO key **+ 4th option `midline`** for cleft palate/neck contracture; 6 cats), region (15 sites incl. pressure-ulcer classics sacrum/ischium/trochanter/heel; 5 cats), urgency (only the 4 trauma-facing cats), intraopEvents (all 12). `approach` deliberately skipped (reconstructionMethod IS the plastics approach axis); burnDepth skipped (encoded in ND92.x diagnoses). aesthetic surgery = intraopEvents only (justified). 31 options (all Arabic) / 36 links / **87 narrowing rows** (recon 43, region 35, laterality 9; tumor-recon ladder un-narrowed). Still-open: burns TBSA% (wants numeric inputType — future framework extension), cleft repair technique, nerve repair type. **PRS: 5q/31opt/36 links/87 narrowing.**
- **VASC questions authored — COMPLETE (migrations 168–169, applied ✅ 2026-07-09)**: save-game `MEDICAL_CODE_AUDITS/VASC/QUESTIONS_VASC.md`. Fresh authoring: 6 questions — `approach` reused with VASC-native semantics (**open / endovascular / hybrid** — all 12 cats), NEW VASC key `graftType` ("Conduit / device used": autologous vein, PTFE/dacron, stent-graft, bare stent, DCB, patch, none; 9 cats, narrowed per category), region "Target vessel / segment" (PAD values match the 2026 CPT LER territories; 4 cats), laterality (9 cats — skipped for midline-aorta AAA/dissection/TAA), urgency (9 cats — skipped for elective-only varicose/AVF/renal), intraopEvents (all 12). 31 options (all Arabic) / 55 links / **57 narrowing rows** (graft 39, region 15, approach 3 — varicose veins can't be "hybrid"). Still-open: structured Fontaine/Rutherford PAD stage + carotid symptomatic status (deferred). **VASC: 6q/31opt/55 links/57 narrowing.**
- **PEDSURG questions authored — COMPLETE (migrations 170–171, applied ✅ 2026-07-09)**: save-game `MEDICAL_CODE_AUDITS/PEDSURG/QUESTIONS_PEDSURG.md`. Fresh authoring: 6 questions — NEW PEDSURG key `ageGroup` (premature neonate→adolescent; 11 cats — skipped where the category pins the age: esoph atresia/abd wall/neonatal emerg=neonate, pyloric=infant), approach with peds-MIS semantics (open/lap/thoracoscopic + explicit conversion options; 11 cats), laterality (5 cats incl. CDH left/right), urgency (only 6 cats — neonatal emergencies=emergency by name skipped), `stomaFormed` reused from GS with peds options (**divided/double-barrel colostomy** = ARM standard; no hartmann; 4 cats), intraopEvents (all 15). abdominal wall defects = intraopEvents only (justified: always neonate/open/urgent). 23 options (all Arabic) / 52 links / **84 narrowing rows** (approach 45, ageGroup 20, stoma 17, laterality 2 — CDH left/right only). Still-open: gastroschisis closure method (primary vs silo), esoph-atresia long-gap staging. **PEDSURG: 6q/23opt/52 links/84 narrowing.**
- **HBP questions authored — COMPLETE (migrations 172–173, applied ✅ 2026-07-09)**: save-game `MEDICAL_CODE_AUDITS/HBP/QUESTIONS_HBP.md`. Fresh authoring: 5 questions — approach with HBP's uniquely multi-modal options (open/lap/robotic/**endoscopic (ERCP)**/**percutaneous**; all 12 cats — acute pancreatitis keeps the full step-up ladder), region "Anatomical site / segment" (liver side/bilobar, cholangio level intrahepatic/perihilar/distal, pancreas head-body-tail, chronic-panc head-vs-diffuse; 7 cats), urgency (6 cats — oncology electives skip), NEW HBP key `childPugh` ("Liver function (Child-Pugh)": non-cirrhotic/A/B/C; HCC + cirrhosis/portal-HTN), intraopEvents (all 12). 24 options (all Arabic) / 39 links / **90 narrowing rows** (approach 68, region 22). Still-open: BDI Strasberg classification, CBD-clearance method. **HBP: 5q/24opt/39 links/90 narrowing.**
- **SOC questions authored — COMPLETE (migrations 174–175, applied ✅ 2026-07-09)**: save-game `MEDICAL_CODE_AUDITS/SOC/QUESTIONS_SOC.md`. Fresh authoring: 9 questions — 3 NEW oncology-universal keys: `surgicalIntent` (curative/palliative/diagnostic-staging/cytoreductive/prophylactic — ALL 16 cats), `nodalSurgery` (none/sampling/sentinel/regional/radical — 12 cats, narrowed per cancer: breast=none/SLNB/ALND, gastric keeps sampling→radical for D1/D2), `neoadjuvant` (none/chemo/radio/CRT/immuno-targeted/hormonal — 9 cats, narrowed: ovarian=chemo only, breast un-narrowed incl. hormonal/targeted) + approach (10 abdominal-pelvic cats), laterality (4), region (5 cats: skin site, sarcoma extremity-vs-retroperitoneal, mets target organ, lymphoma node basin), urgency (colorectal+gastric only), stomaFormed reused **+ urostomy (ileal conduit)** option (colorectal+GU), intraopEvents (all 16). 45 options (all Arabic) / 76 links / **94 narrowing rows** (nodal 37, neo 28, region 20, stoma 9). Still-open: resection-margin R status (post-op field). **SOC: 9q/45opt/76 links/94 narrowing.**
- **MFS questions authored — COMPLETE (migrations 176–177, applied ✅ 2026-07-09)**: save-game `MEDICAL_CODE_AUDITS/MFS/QUESTIONS_MFS.md`. Fresh authoring: 7 questions, **zero new keys — first dept fully composed from established keys**: region "Facial region / jaw" (15 options: mandible subsites, Le Fort, ZMC, orbit, NOE, frontal, panfacial, general jaws, bimaxillary, 3 salivary glands; 7 cats), approach with MFS-native options (**intraoral/extraoral/combined/endoscopic-assisted**; 7 cats — skipped where intraoral-only: impacted/dentoalveolar/implants/orthognathic/cleft), laterality (+midline: NOE/symphysis/cleft palate; 5 cats), urgency (trauma+jaw fx+dentoalveolar infections; 3 cats), fixationMethod (ORTHO key, MFS options: ORIF miniplates/MMF/ORIF+MMF/conservative/ex-fix; 2 cats), nodalSurgery (SOC key, neck-dissection ladder none→radical; oral cancer), intraopEvents (all 12). 38 options (all Arabic) / 37 links / **49 narrowing rows** (region 22, approach 17, laterality 6, fixation 4). **MFS: 7q/38opt/37 links/49 narrowing.**
- **TRS questions authored — COMPLETE (migrations 178–179, applied ✅ 2026-07-09)**: save-game `MEDICAL_CODE_AUDITS/TRS/QUESTIONS_TRS.md`. Fresh authoring: 6 questions — NEW TRS key `donorType` (living / deceased-DBD / deceased-DCD; the 6 recipient transplants, narrowed to deceased-only for heart/lung/pancreas/multi-organ, living kept for liver/renal), approach (only the 2 donor ops — open/lap/hand-assisted/robotic debate), laterality (donor nephrectomy/renal transplant/lung transplant), region "Organ / graft involved" (multi-organ + complications), urgency (rejection + complications), intraopEvents (all 10). Donor operations get approach not donorType (invariantly living). 21 options (all Arabic) / 25 links / **20 narrowing rows** (donorType 12, approach 4, laterality 4). Still-open: rejectionType, complicationType, cold-ischaemia time (single-category/numeric). **TRS: 6q/21opt/25 links/20 narrowing.** Depts remaining for /audit-questions: OBGYN OPHTHAL ENT UROL (+optional NS review).
- **OBGYN questions authored — COMPLETE (migrations 180–181, applied ✅ 2026-07-09)**: save-game `MEDICAL_CODE_AUDITS/OBGYN/QUESTIONS_OBGYN.md`. Fresh authoring: 8 questions spanning obstetric + gynae — approach with OBGYN-native spectrum (abdominal/laparoscopic/robotic/**vaginal**/**hysteroscopic**; 8 cats), laterality (adnexal: ovarian/ectopic/pelvic mass), urgency (5 obstetric emergency cats), NEW OBGYN key `gestationalAge` (first/second/third-preterm/third-term; 4 obstetric cats), + `surgicalIntent`/`nodalSurgery`/`neoadjuvant` reused from SOC for gynaecologic cancer, intraopEvents (all 12). 32 options (all Arabic) / 35 links / **40 narrowing rows** (approach 29, gestAge 9, laterality 2; fibroids approach un-narrowed across all 6 routes). Still-open: miscarriage management mode, perineal-tear degree, FIGO stage. **OBGYN: 8q/32opt/35 links/40 narrowing.** Depts remaining for /audit-questions: OPHTHAL ENT UROL (+optional NS review).
- **ENT questions authored — COMPLETE (migrations 182–183, applied ✅ 2026-07-09)**: save-game `MEDICAL_CODE_AUDITS/ENT/QUESTIONS_ENT.md`. Fresh authoring, **second all-reuse dept (zero new keys, after MFS)**: 8 questions — approach with subsite-specific ENT options (endonasal/transoral/transcervical/transcanal/postauricular/direct laryngoscopy/external; 12 cats), laterality (ear+salivary+thyroid lobe; 6 cats), region "Tumour subsite" (H&N cancer oral/oropharynx/larynx/hypopharynx/nasopharynx), urgency (mastoiditis/laryngeal/H&N airway; 3 cats), surgicalIntent+neoadjuvant reused from SOC (H&N cancer), nodalSurgery reused from MFS neck-dissection ladder (H&N cancer + thyroid), intraopEvents (all 13). otitis media w/ effusion = laterality+intraopEvents only (grommets transcanal). 35 options (all Arabic) / 39 links / **38 narrowing rows** (approach 35, nodalSurgery 3). Still-open: tympanoplasty graft material, OSA surgery level, hearing-loss device type. **ENT: 8q/35opt/39 links/38 narrowing.** Depts remaining for /audit-questions: OPHTHAL UROL (+optional NS review).
- **OPHTHAL questions authored — COMPLETE (migrations 184–185, applied ✅ 2026-07-09)**: save-game `MEDICAL_CODE_AUDITS/OPHTHAL/QUESTIONS_OPHTHAL.md`. Fresh authoring, lean-by-design (technique lives in proc_cpts, so questions are orthogonal axes only): 4 questions — laterality "Eye" (all 12), NEW OPHTHAL key `anesthesiaType` (topical/subconjunctival/local infiltration/sub-tenon/peribulbar/retrobulbar/GA; 11 cats — skipped for refractive=topical; narrowed per category eyelid/orbital=infiltration/GA vs intraocular=topical/blocks), urgency (open globe/mac-on RD/orbital compartment/acute angle-closure glaucoma; 4 cats), intraopEvents (all 12). 14 options (all Arabic) / 39 links / **47 narrowing rows** (all anesthesiaType). Still-open: open-globe zone, corneal graft type, orbital-tumour intent. **OPHTHAL: 4q/14opt/39 links/47 narrowing.** Depts remaining for /audit-questions: UROL (+optional NS review).
- **UROL questions authored — COMPLETE (migrations 186–187, applied ✅ 2026-07-09)**: save-game `MEDICAL_CODE_AUDITS/UROL/QUESTIONS_UROL.md`. Fresh authoring, **third all-reuse dept (zero new keys, after MFS/ENT)**: 8 questions — approach with urology-native options (open/laparoscopic/robotic/**transurethral (endoscopic)**/**percutaneous**; all 13 cats), laterality (kidney/ureter/testis/graft; 6 cats), urgency (acute retention/obstructed system/priapism; 4 cats), surgicalIntent+nodalSurgery+neoadjuvant reused from SOC for the 4 urologic cancers (bladder/prostate/renal/testicular), donorType reused from TRS for renal transplantation, intraopEvents (all 13). 32 options (all Arabic) / 49 links / **65 narrowing rows** (approach 41, nodal 12, neo 10, laterality 2; ureteral obstruction approach un-narrowed). Still-open: stone site/burden, continence device type. **UROL: 8q/32opt/49 links/65 narrowing.**
### ✅ Scaled lectures module — SHIPPED (migrations 188–189, NS copied)
Full design: `docs/LECTURES_SCALED_MODULE_PLAN.md` (gitignored). Scales the flat production NS `lectures` table (152 rows: lectureTitle/google_uid/mainTopic/level) into bilingual per-department academic curriculum in **defaultdb**: `departments → lecture_topics → lectures`.
- **188** CreateLecturesFramework: 2 tables — `lecture_topics` (per-dept curriculum groupings, EN+AR title, sortOrder, UNIQUE(dept,title)) + `lectures` (topicId FK, `lectureNumber` [outline number e.g. "1.3.2" split into its own column], EN+AR title with prefix stripped, `level` msc|md nullable CHECK, sortOrder). `google_uid` dropped.
- **189** CopyNsLectures: copied production NS truth captured 2026-07-09 via `scripts/export-ns-lectures.ts` (values inlined). **10 topics / 152 lectures**; level preserved verbatim (**msc=84, md=62, NULL=6** — 6 source rows had no derivable level, e.g. `1.6.3a`/`3.6.5` lettered/duplicate numbers). `arTitle` left NULL on both tables (no Arabic in source; backfill later). Verified per-topic counts match production (cns tumors=34, spine=26, …); down() cycle clean.
- **New module `src/refLectures/`** (interface/provider/service/controller/router + validator): raw-SQL reads on `ReferenceDataSource` (no entities, no institutionResolver), mirrors `src/refAdditionalQuestions/`. Route (JWT, all 4 roles): `GET /refLectures/department/:deptCode` (+ optional `?level=msc|md`). Legacy tenant `src/lecture/` untouched. Provider smoke test `scripts/smoke-ref-lectures.ts` green (msc filter=84, md filter=62, CTS=0).
- **190** BackfillNsLectureArTitles: filled `arTitle` for all 10 NS topics + 152 lectures (source had none). Medical Arabic; **brain = مخ** per user convention (التشريح الإشعاعي للمخ, خراج المخ). Verified 0 NULL arTitle for NS; down() sets them back to NULL.
- **NB on the 6 NULL `level` rows**: these are NULL in **production too** (stored as empty-string `level=''`, verified 2026-07-09) — the 189 copy is faithful, no data lost. They are the ambiguous lettered/duplicate outline numbers (`1.6.3a`/`1.6.3b`, duplicate `3.6.5`, `5.5.5`/`5.5.6`). Not guessed.
- **`/audit-lectures <DEPT>` skill added** (`.claude/skills/audit-lectures/`): professor-persona authoring of a dept's academic lecture curriculum — consults the specialty's recognised syllabus, picks the topics + lectures, numbers them `X.Y.Z` so the **3rd digit sets the level (Z∈{1,2}→msc, Z∈{3,4}→md, Z≥5/lettered→NULL)**, and authors EN+AR titles inline (brain=مخ). Writes/applies one migration, verifies, records in `MEDICAL_CODE_AUDITS/<DEPT>/LECTURES_<DEPT>.md`. Resumable. `.claude/` is now gitignored (local tooling).
- **CTS lectures — IN PROGRESS (migration 191, more to come)**: transcribed from a **web-found + cited** reference — the **ISCP Cardiothoracic Surgery Curriculum (Aug 2021), Appendix 2 syllabus** (save-game `MEDICAL_CODE_AUDITS/CTS/LECTURES_CTS.md`). **191** = all 20 ISCP topics + cardiac 1–3 lectures (40); **192** = cardiac 4–7 (114). **✅ CTS lectures COMPLETE = 625 lectures / 20 topics** (migrations 191–196). Faithfully transcribed from the ISCP Cardiothoracic Surgery Curriculum (Aug 2021), Appendix 2. All bilingual (0 missing arTitle), all `level = NULL` (MSc/MD to be sourced later per user — never guessed). 191 = 20 topics + cardiac 1–3 (40); 192 = cardiac 4–7 (114); 193 = thoracic 8–10 (114); 194 = thoracic 11–16 (116); 195 = cross-cutting 17–18 (128); 196 = cross-cutting 19–20 (113). down/re-apply tested, tsc clean, query.js 0 flags. Save-game: `MEDICAL_CODE_AUDITS/CTS/LECTURES_CTS.md`. **Nothing committed — awaiting user's explicit ask.** **Key policy decision: `level` (msc/md) is left NULL, never guessed** — the MSc/MD split is a Cairo-University/Egyptian-bylaw construct that international curricula (ISCP/RACS/ACGME) don't carry; it will be filled later from an authoritative Egyptian source (user has a solution). Remaining CTS topics (4–20) = migrations 192–194 (raw ISCP blocks re-extractable via `scripts/tmp-iscp-blocks.js`; `pdf-parse` dep added for this). Numbering: `<topicIdx>.<section>.<n>` (section 1=KNOWLEDGE/CLINICAL, 2=TECHNICAL) — preserves the ISCP structure for the later level-fill without implying a level.
- **`/audit-lectures` skill updated**: (1) finds & cites a published reference via WebSearch/WebFetch (no user input, nothing invented); (2) the **level is set ONLY from an authoritative MSc/MD source** (Egyptian faculty postgraduate bylaw/course-spec — Cairo-matched, or openly-published Mansoura/Ain Shams; or a production `lectures` table), else NULL — **never derived from a heuristic**; (3) `.claude/` gitignored.
- **GS lectures — COMPLETE (migrations 197–199, applied ✅ 2026-07-09)**: transcribed from a **web-found + cited** reference — the **ISCP General Surgery Curriculum (Aug 2021, v2 July 2023), Appendix 2 syllabus** (save-game `MEDICAL_CODE_AUDITS/GS/LECTURES_GS.md`). **11 topics / 493 lectures**, all bilingual (0 missing arTitle), all `level = NULL` (same policy as CTS — MSc/MD not derivable from ISCP; never guessed). Topics = the 11 ISCP modules: elective GS (20), emergency GS (73), upper-GI (72), colorectal (69), general surgery of childhood (18), breast (58), endocrine (19), transplant (41), vascular (4), remote & rural (54), trauma (65). Numbering `<topicIdx>.<section>.<n>` (section 1=KNOWLEDGE/CLINICAL objective topics, 2=TECHNICAL SKILLS). **197** = 11 topics + T1–T3 (165); **198** = colorectal+childhood+breast (145); **199** = endocrine+transplant+vascular+remote+trauma (183). Faithful de-dup: the ISCP "GI & general surgery of childhood" module re-lists UGI/colorectal items → topic 5 keeps only child-specific content (recorded). down/re-apply tested (GS→0→493 identical), tsc clean, query.js 0 missing-AR. **Secondary Egyptian source captured for future `level` backfill: Minia University Master GS course spec 2023 (26 coarse MSc topic-blocks).** **Nothing committed — awaiting user's explicit ask.**
- **ORTHO lectures — COMPLETE (migrations 200–202, applied ✅ 2026-07-09)**: transcribed from a **web-found + cited** reference — the **ISCP Trauma & Orthopaedic Surgery Curriculum (Aug 2021), Appendix 2 syllabus, "Applied Clinical KNOWLEDGE" component** (save-game `MEDICAL_CODE_AUDITS/ORTHO/LECTURES_ORTHO.md`). **10 topics / 397 lectures**, all bilingual (0 missing arTitle), all `level = NULL` (ISCP competence numbers are knowledge-depth 1–4, NOT the Egyptian MSc/MD construct — never guessed). Topics = the 10 ISCP knowledge modules: applied clinical (basic) science (73), foot and ankle (34), knee (31), hip (20), spine (41), hand (31), elbow (27), shoulder (23), trauma (68), paediatric orthopaedic surgery (49). All lectures section 1 (knowledge), numbered `<topicIdx>.1.<n>`. **200** = 10 topics + basic/foot&ankle/knee (138); **201** = hip/spine/hand/elbow (119); **202** = shoulder/trauma/paediatric (140). **Scope decision (documented):** ISCP Part B "Applied Clinical Skills" (~546 hyper-granular trainee-**logbook** procedure-variants, e.g. the 4-way external-fixation/MUA+wires/MUA+POP/ORIF expansions) was reviewed in full and **intentionally excluded** — the source frames it as a surgical logbook, not an academic lecture syllabus; available for a future procedures artifact. Extraction was via a validated parser (PDF Foot&Ankle→Knee interleaving fixed, sub-region prefixes stripped, within-topic dups removed, source typos corrected); Arabic authored via EN→AR map with a zero-missing generator check. down/re-apply tested (ORTHO→0→397 identical), tsc clean. **Secondary Egyptian source for future `level` backfill: Cairo University ortho MSc spec (Orth800, currently 404 after site restructure).** **Nothing committed — awaiting user's explicit ask.**
- **PRS lectures — COMPLETE (migration 203, applied ✅ 2026-07-09)**: transcribed from a **web-found + cited** reference — the **ISCP Plastic Surgery Curriculum (Aug 2021), Appendix 2 "Plastic Surgery Syllabus"** (save-game `MEDICAL_CODE_AUDITS/PRS/LECTURES_PRS.md`). **19 topics / 63 lectures**, all bilingual (0 missing arTitle), all `level = NULL` (ISCP Basic/Intermediate/Advanced are complexity tiers, NOT MSc/MD — never guessed). **Granularity decision (documented):** the reference's own "Summary of Plastic Surgery Syllabus: Module | Sub-section" table maps directly onto the framework — lecture_topics = the 19 MODULES (aesthetic, breast surgery, burns, chest wall recon, cleft, complex wound, craniofacial, craniomaxillofacial trauma, ear recon, genitourinary recon, hand, head & neck, lower limb, oncoplastic breast, pelvic floor recon, skin surgery, vascular anomalies, sarcoma, psychological aspect), lectures = the 63 SUB-SECTIONS (each has its own OBJECTIVE — a teachable unit; 63 OBJECTIVEs verified). The per-sub-section KNOWLEDGE/CLINICAL/TECHNICAL competency bullets are lecture content, not separate lectures (flattening → ~800 hyper-granular items, mis-scaled — same reasoning that scoped GS/ORTHO to teachable units). Single migration (63 rows < 180). All section 1, numbered `<topicIdx>.1.<n>`. down/re-apply tested (PRS→0→63 identical), tsc clean. **Secondary Egyptian source for future `level` backfill: Assiut University plastic-surgery MSc spec.** **Nothing committed — awaiting user's explicit ask.**
- **VASC lectures — COMPLETE (migration 204, applied ✅ 2026-07-09)**: transcribed from a **web-found + cited** reference — the **ISCP Vascular Surgery Curriculum (Aug 2021), Appendix 2 "Vascular Surgery Syllabus"** (save-game `MEDICAL_CODE_AUDITS/VASC/LECTURES_VASC.md`). **5 topics / 47 lectures**, all bilingual (0 missing arTitle), all `level = NULL` (ISCP Phase-2/Phase-3 markers are training-phase tiers, NOT MSc/MD — never guessed). **Granularity decision (documented):** the syllabus's two-level structure maps onto the framework — lecture_topics = the 5 SECTION groupings (vascular surgery generic topics [12], vascular surgery imaging [5], generic procedures 1 [1], generic procedures 2 [18], abdominal & general surgery topics [11]), lectures = the 47 ALL-CAPS topics (each a teachable unit: carotid artery disease, aneurysm-elective/emergency, vascular ultrasound, etc.; 47 OBJECTIVEs verified). Per-topic OBJECTIVES competency bullets are lecture content, not separate lectures (same scoping as GS/ORTHO/PRS). Section names kept faithful (incl. the awkward "generic procedures 1/2"). Single migration (47 rows). All section 1, numbered `<topicIdx>.1.<n>`. down/re-apply tested (VASC→0→47 identical), tsc clean. **No Egyptian vascular MSc spec was retrievable in search — level backfill deferred.** **Nothing committed — awaiting user's explicit ask.**
- **PEDSURG lectures — COMPLETE (migration 205, applied ✅ 2026-07-09)**: transcribed from a **web-found + cited** reference — the **ISCP Paediatric Surgery Curriculum (Aug 2021), Appendix 2 "Syllabus"** (save-game `MEDICAL_CODE_AUDITS/PEDSURG/LECTURES_PEDSURG.md`). **9 topics / 62 lectures**, all bilingual (0 missing arTitle), all `level = NULL` (ISCP competence numbers are knowledge-depth 1–4, NOT MSc/MD — never guessed). **Granularity decision (documented):** the syllabus's Category → Topic structure maps onto the framework — lecture_topics = the 9 CATEGORIES (general surgery of childhood [3], gastrointestinal [9], urology [12], neonatal surgery [13], oncology [7], endocrine conditions [6], thoracic anomalies [4], operative skills [3], management [5]), lectures = the 62 TOPICS (each has its own Objective: pyloric stenosis, congenital diaphragmatic hernia, posterior urethral valves, wilms tumour, etc.; 62 "Topic" entries verified). Per-topic Knowledge/Clinical/Technical bullets are lecture content, not separate lectures (same scoping as GS/ORTHO/PRS/VASC). 3 PDF wrap-repairs (bladder exstrophy full title; lung abnormalities incl. empyema; adrenal gland). Single migration (62 rows). All section 1, numbered `<topicIdx>.1.<n>`. down/re-apply tested (PEDSURG→0→62 identical), tsc clean. **No Egyptian paediatric-surgery MSc spec retrievable in search — level backfill deferred.** **Nothing committed — awaiting user's explicit ask.**
- **HBP lectures — COMPLETE (migration 206, applied ✅ 2026-07-09)**: transcribed from a **web-found + cited** reference — the **IHPBA "Curriculum for Hepato-Pancreato-Biliary Surgery Fellowships" (International Hepato-Pancreato-Biliary Association, 2008)** (save-game `MEDICAL_CODE_AUDITS/HBP/LECTURES_HBP.md`). Chosen because HBP is a **subspecialty**, not a standalone ISCP specialty — the IHPBA fellowship curriculum is the authoritative international society HPB curriculum (avoids overlapping the GS lectures). **7 topics / 65 lectures**, all bilingual (0 missing arTitle), all `level = NULL` (fellowship curriculum has no MSc/MD tiering — never guessed). **Granularity decision (documented):** the curriculum's "7 Major Units, some with Subunits" structure maps onto the framework — lecture_topics = the 7 UNITS (the liver [20], the biliary tract incl. gallbladder [13], the pancreas and duodenum [19], imaging [3], oncology [4], trauma [3], transplantation [3]), lectures = the top-level Content study-areas (physiology of the liver, gallstones, pancreatitis, benign neoplasms, etc.); deeper Content sub-bullets + Clinical Skills are lecture content. Parser keyed to the expected next content-letter (rejects deep re-used letters/romans) + 3 manual corrections (dropped spurious "sugiura procedure"; added "organ preservation"/"transplantation" from a broken a/d/e lettering; trimmed a trailing "including"). Single migration (65 rows). All section 1, numbered `<topicIdx>.1.<n>`. down/re-apply tested (HBP→0→65 identical), tsc clean. **No Egyptian HBP MSc spec retrievable in search — level backfill deferred.** **Nothing committed — awaiting user's explicit ask.**
- **SOC lectures — COMPLETE (migration 207, applied ✅ 2026-07-09)**: transcribed from a **web-found + cited** reference — the **ESSO Core Curriculum ("The ESSO core curriculum committee update on surgical oncology", European Society of Surgical Oncology, EJSO 2021)** (save-game `MEDICAL_CODE_AUDITS/SOC/LECTURES_SOC.md`). Chosen because SOC is a **subspecialty** (not a standalone ISCP specialty) — the ESSO core curriculum is the authoritative European society curriculum (built for the EBSQ). **2 topics / 21 lectures**, all bilingual (0 missing arTitle), all `level = NULL` (ESSO has no MSc/MD tiering — never guessed). **Granularity decision (documented):** the ESSO curriculum is a numbered outline; lecture_topics = the 2 academic-content sections (basic principles of oncology [10: 1.1–1.10]; disease site specific oncology [11: 2.1–2.11]), lectures = its numbered subsections carrying the reference's OWN outline numbers (carcinogenesis, epidemiology, radiation biology, …; breast/colorectal/thoracic/upper-GI/HPB/skin-melanoma/urological/endocrine/sarcoma/gynaecological/peritoneal cancer). Per-subsection two-column Basic/Advanced knowledge tables are lecture content (interleave in the PDF, not cleanly splittable). Excluded (non-lecture): §3 Generic Clinical Skills (empty stub), §4 Training Recommendations (programme-design), §5 EBSQ Eligibility (admin). Single migration (21 rows). down/re-apply tested (SOC→0→21 identical), tsc clean. Compact by design — faithful to the ESSO 2-pillar structure. **No Egyptian surgical-oncology MSc spec retrievable — level backfill deferred.** **Nothing committed — awaiting user's explicit ask.**
- **MFS lectures — COMPLETE (migrations 208–210, applied ✅ 2026-07-09)**: transcribed from a **web-found + cited** reference — the **ISCP Oral and Maxillofacial Surgery Curriculum (Aug 2021), Appendix 2 syllabus** (save-game `MEDICAL_CODE_AUDITS/MFS/LECTURES_MFS.md`). **12 topics / 675 lectures — the LARGEST dept** (bigger than CTS 625), all bilingual (0 missing arTitle), all `level = NULL` (ISCP P2/P3/SI are training-phase tiers, not MSc/MD — never guessed). **Granularity decision (documented, same as ORTHO/GS):** lecture_topics = the 12 clinical MODULES (airway 41, craniofacial trauma 56, jaw deformity 67, facial pain & tmj 40, head & neck 63, salivary glands 45, oral mucosa 46, skin 49, restoration of aesthetic form 69, cleft lip & palate 65, craniofacial 78, dentoalveolar 56), lectures = each module's competence-graded syllabus topic-lines; generic sub-group headers (anatomy/pathology/etc.) are groupers, not lectures. The separate generic "Common Content Module" (Core Surgical Training Phase-1 basic science, shared across all surgical specialties) is **excluded**; two modules lacking a "Module" suffix ("conditions of the oral mucosa", "conditions of the skin") were included. Extraction via a parser (competence-line detection + wrapped-line joining + grouper skip + lowercase-continuation heuristic), validated against raw: fixed the salivary/oral-mucosa boundary, 4 uppercase sub-header merges, dropped 7 PDF fragments; Arabic authored via EN→AR map with a zero-missing generator check. 3 migrations (204/203/268). All section 1, numbered `<topicIdx>.1.<n>`. down/re-apply tested (MFS→0→675 identical), tsc clean. **No Egyptian OMFS MSc spec retrievable (OMFS is a dental-faculty specialty in Egypt) — level backfill deferred.** **Nothing committed — awaiting user's explicit ask.**
- **TRS lectures — COMPLETE (migration 211, applied ✅ 2026-07-10)**: transcribed from a **web-found + cited** reference — the **UEMS Section of Surgery, Division of Transplantation "Curriculum and Syllabus — Transplantation" (European Board of Surgery / EBSQ in Transplant Surgery, 2021)** (save-game `MEDICAL_CODE_AUDITS/TRS/LECTURES_TRS.md`). Chosen because TRS is a **subspecialty** (not a standalone ISCP specialty) — the UEMS EBSQ curriculum is the authoritative European board curriculum (avoids overlapping GS/HBP). **4 topics / 54 lectures**, all bilingual (0 missing arTitle), all `level = NULL` (UEMS has no MSc/MD tiering — never guessed). **Granularity decision (documented):** lecture_topics = the 4 MODULES (multi-organ retrieval 12, kidney transplantation 15, pancreas transplantation 13, liver transplantation 14), lectures = each module's Knowledge (section 1) + Clinical skills (section 2) bullets, numbered `<topic>.<section>.<n>`. Excluded (non-module-specific): the generic Professional-skills competency overlay (communication/teamwork/audit/medico-legal/databases, repeated verbatim per module) + the Subject objectives. Single migration (54 rows). down/re-apply tested (TRS→0→54 identical), tsc clean. **No Egyptian transplant-surgery MSc spec retrievable — level backfill deferred.** **Nothing committed — awaiting user's explicit ask.**
- **OBGYN lectures — COMPLETE (migrations 212–213, applied ✅ 2026-07-10)**: transcribed from a **web-found + cited** reference — the **RCOG "MRCOG Syllabus and Knowledge Requirements for Core Curriculum" (Royal College of Obstetricians and Gynaecologists, 2019)** (save-game `MEDICAL_CODE_AUDITS/OBGYN/LECTURES_OBGYN.md`). Chosen because the RCOG Curriculum 2024 is CiP/competency-based (not a topic list) while the MRCOG Syllabus enumerates 15 core Knowledge Areas with Detailed Knowledge Requirements. **13 topics / 217 lectures**, all bilingual (0 missing arTitle), all `level = NULL` (MRCOG Part-stage competence tiers, not MSc/MD — never guessed). **Granularity decision (documented):** lecture_topics = the MRCOG Knowledge Areas, lectures = their Detailed Knowledge Requirement topics (core surgical skills 26, postoperative care 6, antenatal care 26, maternal medicine 18, management of labour 35, management of delivery 25, postpartum problems 27, gynaecological problems 12, subfertility 7, sexual & reproductive health 16, early pregnancy care 5, gynaecological oncology 8, urogynaecology 6). Excluded the 2 generic professional Knowledge Areas (KA1 Clinical skills, KA2 Teaching & research). Extraction via a parser (Detailed-section-only; stripped repeated "understand the epidemiology… of <condition>" boilerplate to the condition name; reduced "Topic: sub-list" compounds to the topic name; dropped boilerplate summary bullets + Part-1/2/3 competency statements); Arabic via EN→AR map with a zero-missing generator check. Known gap: gynae-oncology specific cancers were only a compound MRCOG summary bullet (dropped), so that KA is thinner. 2 migrations (111/106). down/re-apply tested (OBGYN→0→217 identical), tsc clean. **No Egyptian OBGYN MSc spec retrievable — level backfill deferred.** **Nothing committed — awaiting user's explicit ask.**
- **ENT lectures — COMPLETE (migration 214, applied ✅ 2026-07-10)**: transcribed from a **web-found + cited** reference — the **ISCP Otolaryngology Curriculum (Aug 2021), Appendix 2 syllabus** (save-game `MEDICAL_CODE_AUDITS/ENT/LECTURES_ENT.md`). **4 topics / 60 lectures**, all bilingual (0 missing arTitle), all `level = NULL` (ISCP P2/P3/SI are training-phase tiers, not MSc/MD — never guessed). **Granularity decision (documented, same as PEDSURG):** lecture_topics = the ENT subspecialty CATEGORIES (paediatric otolaryngology 18, otology 10, rhinology 16, head and neck 16 — Laryngology and Thyroid/Parathyroid filed under Head and Neck), lectures = the Topic entries (each with its own Objective); per-topic Knowledge/Clinical/Technical bullets are lecture content. Anomalous singleton Category values (all SI = Rh) placed under Rhinology; "Skin cancer" under Head and Neck; one duplicate ("orbital disorders") deduped; "P2 P3 SI" competence-marker suffixes stripped from titles. Single migration (60 rows). All section 1, numbered `<topicIdx>.1.<n>`. down/re-apply tested (ENT→0→60 identical), tsc clean. **No Egyptian ENT MSc spec retrievable — level backfill deferred.** **Nothing committed — awaiting user's explicit ask.**
- **UROL lectures — COMPLETE (migration 216, applied ✅ 2026-07-10)**: transcribed from a **web-found + cited** reference — the **ISCP Urology Curriculum (Aug 2021), Appendix 2 "Urology Syllabus"** (save-game `MEDICAL_CODE_AUDITS/UROL/LECTURES_UROL.md`). **7 topics / 87 lectures**, all bilingual (0 missing arTitle), all `level = NULL` (ISCP P2/P3/SI training-phase + knowledge-depth 1–4 tiers, not MSc/MD — never guessed). **Granularity decision (documented):** the syllabus has three top-level parts, each a topic — certification competencies (19 "manage the patient with…" objectives), knowledge (32: basic science anatomy/physiology/pharmacology/pathology + 28 condition-blocks), and the 5 Special-Interest MODULES (general urology 8, female/functional/reconstructive 11, endourology 4, andrology & infertility 7, urological oncology 6); lectures = each grouping's enumerated topic entries, per-topic knowledge/clinical/technical bullets are lecture content (same scoping as ENT/PEDSURG). The reference's deliberate cross-part repeats (e.g. prostate/bladder/renal cancer in both knowledge and oncology module) kept faithfully (no lecture uniqueness). Single migration (87 rows < 180), all section 1, numbered `<topicIdx>.1.<n>`. down/re-apply tested (UROL→0→87 identical), tsc clean, query.js 0 missing-AR. **No enumerated Egyptian urology MSc spec retrievable (Mansoura awards MSc/MD but publishes no topic list) — level backfill deferred.** **Nothing committed — awaiting user's explicit ask.**
- **OPHTHAL lectures — COMPLETE (migrations 215 + 217, applied ✅ 2026-07-10)**: transcribed from a **source-0 Egyptian faculty postgraduate curriculum** — the **"Master (MSC) Degree Program and Courses Specifications for Ophthalmology", Department of Ophthalmology, Faculty of Medicine, Assiut University (2021-2022/2022-2023, approved 27-11-2022)** (save-game `MEDICAL_CODE_AUDITS/OPHTHAL/LECTURES_OPHTHAL.md`). **8 topics / 219 lectures**, all bilingual (0 missing arTitle). **First lectures dept with `level` authoritatively set — all `level = 'msc'`** because the document is explicitly the Master (MSC) programme (sourced, NOT guessed; the Assiut MD/Doctorate spec could add `md` content later). **Granularity decision:** lecture_topics = the reference's own top-level divisions (the 6 courses, with Course 6 "Ophthalmology" split into its 3 modules) → anatomy of the eye 19, physiology of the eye 25, optics and refraction 16, general surgery 8, internal medicine and neurological diseases 11, eye medicine 77, eye surgery 46, eye pathology 17; lectures = the enumerated items in each course's "Course contents (topics/modules)" tables + Module-1/2 ILO disease lists. For the clinical modules the outline-number chapter digit preserves the source's subspecialty grouping (refraction/cornea/glaucoma/…). Two migrations (>180 rows): 215 = 8 topics + topics 0–5 lectures (156); 217 = eye surgery + eye pathology (63). down/re-apply tested (217 revert OPHTHAL 219→156→219 identical, sibling's 216 untouched), tsc clean, query.js 0 missing-AR, no flags. **Nothing committed — awaiting user's explicit ask.**
- **🎉 LECTURES FRAMEWORK COMPLETE — all 15 non-NS departments authored** (CTS, GS, ORTHO, PRS, VASC, PEDSURG, HBP, SOC, MFS, TRS, OBGYN, ENT, UROL, OPHTHAL; NS is the production mirror). Every dept web-found + cited, all bilingual (0 missing arTitle). **`level`**: all NULL except **OPHTHAL = all `msc`** (sourced from the Assiut Master ophthalmology curriculum — the only source-0 Egyptian faculty spec located; first authoritative level). **Open (later steps, per user)**: backfill `level` (msc/md) for the 14 NULL depts from an authoritative Egyptian MSc/MD source or the production tenant `lectures` table (never guessed); optional NS review; optional OPHTHAL `md`-level content from the Assiut Doctorate spec.

- **🎉 QUESTIONS FRAMEWORK COMPLETE — all 15 departments configured.** Grand total: **98 questions / 472 options / 700 links / 937 narrowing rows**, 0 NULL arValue. Full-framework integrity verified on staging (2026-07-09): 0 cross-dept link violations, 0 orphan narrowing rows, 0 choice-questions-without-options, 0 options-on-free_text. 16 distinct question keys reused across depts (approach, laterality, region, urgency, position, intraopEvents, clinicalPresentation, surgicalDomain, cpbStrategy, woundClass, stomaFormed, fixationMethod, fractureType, reconstructionMethod, graftType, ageGroup, childPugh, donorType, surgicalIntent, nodalSurgery, neoadjuvant, gestationalAge, anesthesiaType). Migrations `157`–`187`. **NS remains the untouched production mirror (optional future review).**

### ✅ Last two cross-dept mislabels fixed (migration 156)
Resolved the final two open cross-dept ICD-11 mislabels (verified via findacode ICD-11 MMS; both collision-free in-place recodes, links preserved by row id, names unchanged → embeddings stayed valid, no backfill):
- **GS `2B90.Y`="Lynch syndrome"** → **`QC61.0`** "Family history of malignant neoplasm of digestive organs". `2B90.Y` = *Other specified malignant neoplasms of colon* (colon adeno already covered by `2B90`+`2B90.Z`); ICD-11 has no dedicated Lynch leaf, so the family-history block is the closest verifiable home (≈ Z15.0/Z80.0). Name kept.
- **CTS `CA22.Z`="lung abscess"** → **`CA43.2`** "Abscess of lung without pneumonia" (canonical lung-abscess leaf, ≈ J85.2). `CA22.Z` = *COPD unspecified* (already covered by `CA22` [TRS]). Name kept.

**Only remaining open data item project-wide: the NS `2F7C`/`2F7Z` re-review** (hemangioblastoma / epidermoid-dermoid on uncertain-behaviour codes).

### ✅ UROL full dept-audit — COMPLETE (migrations 145–151)
Save-game at `MEDICAL_CODE_AUDITS/UROL/AUDIT_UROL.md`. UROL (Urology) reference data was **~74% corrupt (16/23 ICD codes wrong + 1 invalid leaf)** and started with **0 proc_cpts** and only 23 diagnoses. The seed mis-chaptered urological conditions onto **female-genital GA** codes (GA00=vulvitis, GA20, GA40), **cystitis GC00.x** (used for renal/bladder/ureter stones), and **nephritis GB4x/GB5x** blocks (GB40=nephritic, GB42, GB50=acute TIN, GB51=acute pyelonephritis), plus oncology mis-codes (RCC on ovary code 2C73; seminoma on vulva-melanoma code 2C70.1).
- **145** MIG-A: 16 ICD fixes = **7 collision-aware MERGEs** into existing shared rows (RCC `2C73.0`→`2C90.0` SOC; seminoma `2C70.1`→`2C80.2` PEDSURG/SOC; varicocele `GB40.0`→`BD75.1` PEDSURG; torsion `GB42.0`→`GB01.0` PEDSURG; VUR `GC10.0`→`GB56.5` TRS; OAB `GC80.0`→`GC50.0` OBGYN [dual incont+retention]; stress incontinence `GA00.0`→`MF50.20` OBGYN) + **8 free in-place recodes** (pyelonephritis→`GB51`; 3 stones→`GB70.0Z`/`GB71.0`/`GB70.1`; phimosis→`GB05.2`; epididymo-orchitis→`GB02.1`; hydronephrosis→`GB56.4`; UPJO→`GB56.0`; urethral stricture→`GC03`) + **1 invalid-leaf** (BPH `GA90.0`→`GA90`).
- **146+147+148** +77 distinct diagnoses → **100 total** (148 caught 2 bladder-cancer stragglers — interstitial + radiation cystitis). 71 embeddings backfilled. Every category ≥5. ("oligozoospermia" has no ICD-11 entry — used cryptorchidism/Klinefelter/testicular-atrophy for infertility.)
- **149+150** Imported **103** AAPC-verified urology proc_cpts across **13 new alpha groups** (TURP, PROS, CYST, URET, STON, NEPH, RTPX, ORCH, VARI, PENI, EREC, INCO, RETN). **Deletion caught**: `55700` (prostate needle biopsy, deleted 2026-01-01) excluded → `55706` transperineal saturation biopsy used. 99 proc embeddings backfilled.
- **151** Linked all 103 procs + MNR to the 13 main_diags (dual-link: stent `52332`→ureteral obstruction + nephrolithiasis).

**State after 151: UROL at 100 diagnoses (all ICD-11 verified ✅, all embedded ✅), 104 linked proc_cpts (103 dept-specific AAPC-verified + MNR, all embedded ✅), 13 main_diags; 0 orphans, 0 empty categories. Every category ≥5 diagnoses & ≥7 procs. Audit complete.**

> ⚠️ No NEW cross-dept mismaps found in UROL (all 7 MERGE targets were correctly-labelled shared rows). **With UROL done, every department now has proc_cpts.** Remaining open project-wide: GS `2B90.Y`="Lynch syndrome"; `CA22.Z`="lung abscess" (respiratory pass); NS `2F7C`/`2F7Z` re-review.

### ✅ OPHTHAL full dept-audit — COMPLETE (migrations 139–144)
Save-game at `MEDICAL_CODE_AUDITS/OPHTHAL/AUDIT_OPHTHAL.md`. OPHTHAL (Ophthalmology) reference data was **~64% corrupt (15/28 ICD codes wrong + 3 parent→leaf)** and started with **0 proc_cpts** and 28 diagnoses. The seed used a fabricated `9A00/9A20/9A30/9A40/9A50/9A81/9B11/9B20/9B40/9B41` scheme not matching WHO ICD-11 (only the cornea `9A7x`, refractive `9D00.x` and strabismus `9C80.x` blocks were correct).
- **139** MIG-A: 18 fixes = 14 in-place recodes (glaucoma→`9C61`, conjunctiva→`9A60/9A61`, corneal ulcer→`9A76`, retinal vascular→`9B74`, retinal detachment→`9B73`, vitreous→`9B83`, thyroid orbitopathy→`9C82.3`, retinoblastoma→`2D02.2`) + 3 parent→leaf + **1 cross-dept MERGE** (diabetic retinopathy `9A50.0`→existing **TRS** `9B71.0Z`) + structural relink (conjunctivitis `9A60.Z` moved eyelid→pterygium/ocular-surface). NB: recoding vitreous-haem `9B11.0`→`9B83` freed `9B11.0`, reused for aphakia.
- **140+141** +72 distinct diagnoses → **100 total** (cataract +8, cornea +8, glaucoma +8, macula +5, diabetic retinopathy +6, retina +5, eyelid +6, orbit +6, ocular-surface +4, refractive +4, strabismus +5, trauma +7). 85 embeddings backfilled. **Resolved the long-open amblyopia item: amblyopia = `9D46`.**
- **142+143** Imported **104** AAPC-verified proc_cpts across **12 new alpha groups** (CATR, CORN, GLAU, RETN, INJX, STRB, OPLT, LACR, ORBT, PTRY, REFR, OTRA). None deleted; 2 Cat-III codes verified active (0402T cross-linking, 0671T MIGS stent). 104 proc embeddings backfilled.
- **144** Linked all 104 procs + MNR to the 12 main_diags (some dual-linked: INJX/RETN across diabetic-retinopathy/macula/RD; CORN 65772 & CATR 66984 → refractive; CORN 65286/65222 & CATR 67005 → ocular trauma).

**State after 144: OPHTHAL at 100 diagnoses (all ICD-11 verified ✅, all embedded ✅), 105 linked proc_cpts (104 AAPC-verified active + MNR, all embedded ✅), 12 main_diags; 0 orphans, 0 empty categories. Every category ≥5 diagnoses & ≥7 procs. Audit complete.**

> ⚠️ No new cross-dept mismaps found in OPHTHAL. Diabetic retinopathy `9B71.0Z` now correctly shared **OPHTHAL+TRS**. Only remaining dept without proc_cpts: **UROL**.

### ✅ ENT full dept-audit — COMPLETE (migrations 132–138)
Save-game at `MEDICAL_CODE_AUDITS/ENT/AUDIT_ENT.md`. ENT (Otolaryngology) was the **least-corrupted department audited yet (~14%, 4/29 ICD codes wrong + 4 approximate)** — the bulk of its original seed mismaps had already been fixed by migrations 026/035. Started with **29 diagnoses and 0 proc_cpts**.
- **132** MIG-A: 2 cross-dept MERGEs (parotid calculus `DA50.2`→`DA04.4` sialolithiasis MFS-owned; branchial cyst `DA50.3`→`DA05.Y` branchial-cleft-cyst PEDSURG-owned) + 6 in-place recodes (deviated septum `CA01.0`→`CA0D`; **OSA `CA62.0`→`7A41`** — OSA moved to ch.7 sleep-wake disorders; sudden SNHL `AB51.1`→`AB55`; 3 parent→leaf: `CA0J`→`CA0J.Z`, `AB13`→`AB13.Z`, `CA03`→`CA03.Z`) + 3 name/AR fixes (the "MeniÃ¨re" mojibake, cholesteatoma & vocal-cord-polyp Arabic).
- **133+134+135** +73 distinct diagnoses → **102 total** (ear/hearing +23, nose/sinus/larynx/pharynx/tonsil/OSA +29, H&N cancer/thyroid/salivary +21). Several rows shared with MFS/SOC/NS/PEDSURG (reused via ON CONFLICT). 64 embeddings backfilled. Skipped inverted papilloma (its code `2F7Z` occupied by NS's mislabelled epidermoid/dermoid row).
- **136+137** Imported **81** AAPC-verified ENT proc_cpts across **11 new alpha groups** (FESS, SEPT, NASF, TONS, OSAS, MYRT, EARC, TYMP, MAST, STAP, LARY) + 3 parathyroid/substernal codes added to the shared THYR group + 1 to shared HNCK. None deleted by AMA.
- **138** Linked all 105 procs (81 imported + **24 reused shared** GS/SOC/MFS thyroid/salivary/neck rows) + MNR to the 13 main_diags. 81 new proc embeddings backfilled.

**State after 138: ENT at 102 diagnoses (all ICD-11 verified ✅, all embedded ✅), 106 linked proc_cpts (105 AAPC-verified active + MNR, all embedded ✅), 13 main_diags; 0 orphans, 0 empty categories. Every category ≥5 diagnoses & ≥5 dept-specific procs. Audit complete.**

> ⚠️ No new cross-dept mismaps found in ENT. The only remaining open cross-dept item project-wide is **GS `2B90.Y`="Lynch syndrome"** (fix in a GS pass). (Also still open: `CA22.Z`="lung abscess" mislabel flagged during the TRS audit.)

### ✅ OBGYN full dept-audit — COMPLETE (migrations 124–131)
Save-game at `MEDICAL_CODE_AUDITS/OBGYN/AUDIT_OBGYN.md`. OBGYN (Obstetrics & Gynaecology) reference data was **~78% corrupt (15/23 ICD codes wrong + 3 approximate)** and started with **0 proc_cpts** and only 23 diagnoses. Gynae conditions were scattered into the wrong chapter blocks (leiomyoma in the GA10 endometriosis block, PCOS outside endocrine 5A80, ovarian cyst/torsion/PID/prolapse mis-coded) plus an obstetric tangle (pre-eclampsia/eclampsia swapped, PPH on a preterm-labour code, uterine rupture on a puerperal-sepsis code).
- **124** MIG-A: 18 in-place recodes (all OBGYN-only). Resolved the long-flagged **cervix mismap** `2C76.0`→`2C77.Z` (2C76.0 = endometrial, not cervix); endometrial `2C76.1`→`2C76.Z`; eclampsia `JA24.0`(=mild-mod pre-ec)→`JA25.3`; PPH `JB00.0`(=preterm labour)→`JA43.Z`; uterine rupture `JB40.0`(=puerperal sepsis)→`JB0A.1`; PCOS `GA15.4`→`5A80.1`; leiomyoma `GA10.0`→`2E86.0`; etc.
- **125+126+127** +81 distinct diagnoses → **104 total** (obstetric +38, gynae cancer +15, ovarian/pelvic/endometriosis +12, fibroids/prolapse/SUI +16). Three freed codes reused to their correct WHO meaning (GA15.0→cervix polyp, JB40.0→puerperal sepsis, JB00.0→preterm labour). 95 embeddings backfilled.
- **128+129** Imported **100** AAPC-verified proc_cpts across **12 new alpha groups** (CSEC, VDEL, ECTO, HYST, MYOM, HYSC, DILC, ADNX, ONCO, PROL, INCO, CERV). HYST/ONCO overlap SOC's existing rows (reused via ON CONFLICT). Deleted code 58823 caught → replaced with 58820. 100 proc embeddings backfilled.
- **130+131** Linked all 100 procs + MNR to the 12 main_diags (131 cleaned up 18 procs unlinked by 130).

**State after 131: OBGYN at 104 diagnoses (all ICD-11 verified ✅, all embedded ✅), 101 linked proc_cpts (100 dept-specific AAPC-verified + MNR, all embedded ✅), 12 main_diags; 0 orphans, 0 empty categories. Every category ≥5 diagnoses & ≥6 procs. Audit complete.**

> ⚠️ The **OBGYN cervix mismap is now RESOLVED** (open since the SOC audit). The only remaining open cross-dept mismap is **GS `2B90.Y`="Lynch syndrome"** (fix in a GS pass).

### ✅ TRS full dept-audit — COMPLETE (migrations 117–123)
Save-game at `MEDICAL_CODE_AUDITS/TRS/AUDIT_TRS.md`. TRS (Transplant Surgery) was the **second-most corrupt department (~80%, 15/20 ICD codes wrong)** and started with **0 proc_cpts** and only 20 diagnoses. Liver disease was mis-chaptered into the metabolic chapter (`5C56.x`/`5C81.3`), PBC into the developmental chapter (`LB41.1`), and rejection/complication subdivisions (`NE84.0/.1`, `NE80.0`, `NE81.0`, `NE85.0`) plus several CKD-stage codes (`GB60.1`, `GB61.0`, `GB62.0`, `GB63.0`) were fabricated/mislabelled.
- **117** MIG-A: 15 ICD-11 fixes = 3 collision-aware MERGEs (ESLD `5C56.0`→`DB93.1`; PBC `LB41.1`→`DB96.1Z`; hepatic vein thrombosis `5C81.3`→`DB98.5` Budd-Chiari) + 12 in-place recodes incl. the **cross-dept PKD fix `GB63.0`→`GB81`** (corrects shared **TRS+UROL**), and the rejection codes onto `NE84` + acuteness extensions (`NE84&XT5R`/`&XT8W`). CA22 (COPD) left as-is — leaf `CA22.Z` is occupied by a mislabelled "lung abscess" row (new open item).
- **118+119+120** +83 distinct diagnoses → **103 total** (liver +19, renal +14, heart +10, lung +14, pancreas +4, multi-organ +4, rejection +2, complications +13, donor +3; shared rows reused via ON CONFLICT — HCC/T2DM/PSC/hepatorenal). 75 embeddings backfilled.
- **121+122** Imported **100** AAPC-verified transplant proc_cpts across **12 new alpha groups** (LIVT, KTNP, PANT, HRTT, LUNT, INTT, DIAL [shared with VASC], BILR, VASR, PORT, IMMB, COMP). 90 proc embeddings backfilled.
- **123** Linked all 100 procs + MNR to the 10 main_diags; every category ≥6 procs.

**State after 123: TRS at 103 diagnoses (all ICD-11 verified ✅, all embedded ✅), 101 linked proc_cpts (100 dept-specific AAPC-verified + MNR, all embedded ✅), 10 main_diags; 0 orphans, 0 empty categories. Every category ≥5 diagnoses & ≥6 procs. Audit complete.**

> ⚠️ **New open item (CA22.Z mislabel)**: `CA22.Z` is labelled "lung abscess - unspecified" in the shared DB but `CA22.Z` = COPD unspecified. Flag for a respiratory/pulmonary pass (likely a CTS/ENT seed row). See `MISMAPPED_ICD11_CODES.md`.

### ✅ MFS full dept-audit — COMPLETE (migrations 111–116)
Save-game at `MEDICAL_CODE_AUDITS/MFS/AUDIT_MFS.md`. MFS (Maxillofacial Surgery) was the **most corrupt department yet (~86%, 18/21 ICD codes wrong)**: facial fractures mis-chaptered into the `DA0x` dental-disease chapter (belong `NA02.x` injury), fabricated `DA4x` cyst/tumour codes, mis-coded cleft, and the long-flagged **oral-cancer mismap** (oncology codes were pancreas/liver/gallbladder codes). MFS started with **0 proc_cpts**.
- **111** MIG-A: 13 recodes + 3 MERGEs (clefts→PRS `LA40.Z`/`LA42.Z`; dentigerous→own `DA05.0`) + **2 cross-dept MERGEs** resolving the oral-cancer mismap: `2C10.0` row *renamed*→"adenocarcinoma of pancreas" (**fixes GS**), `2C13.0` row *renamed*→"adenocarcinoma of gallbladder" (**fixes HBP**), MFS relinked to SOC `2B66.0`/`2B68.Z`. (`2C10.1`/`2C12.0` recoded→lip/tongue `2B60.Z`/`2B62.Z`.)
- **112+113** +80 diagnoses → **100 total** (73 new rows + 7 shared links: PRS cleft subtypes, SOC lip/tongue SCC, ORTHO MRONJ). 88 embeddings backfilled. Every category ≥5.
- **114+115** Imported **104** AAPC-verified oral/maxillofacial proc_cpts across **11 alpha groups** (JCYS, CLEF, IMPL, VEST, DTAL, ORIF, ARCH, ORTH, TMJS, SALV, ONCO) + 10 reused shared rows (PRS CLEF, SOC HNCK, PRS EXCN). **Deletion caught**: `21310` (closed nasal fx, deleted 2022-01-01) excluded.
- **116** Linked all 104 procs + MNR to the 12 main_diags. 94 new proc embeddings backfilled.

**State after 116: MFS at 100 diagnoses (all ICD-11 verified ✅, all embedded ✅), 105 linked proc_cpts (104 AAPC-verified oral/maxillofacial ops + MNR, all embedded ✅), 12 main_diags; 0 orphans, 0 empty categories. Every category ≥5 diagnoses & ≥7 procs. Audit complete.**

> ⚠️ The **MFS oral-cancer mismap is now RESOLVED** (open since the HBP audit) — and the fix also corrected the shared **GS `2C10.0`** and **HBP `2C13.0`** rows. Two mismaps from the SOC audit remain open: **GS `2B90.Y`="Lynch syndrome"** and **OBGYN `2C76.0`="cervix"** (fix in those depts' audits).

### ✅ SOC full dept-audit — COMPLETE (migrations 104–110)
Save-game at `MEDICAL_CODE_AUDITS/SOC/AUDIT_SOC.md`. SOC (Surgical Oncology) reference data was **~62% corrupt** (13/26 ICD codes wrong + 3 approximate). The seed mapped concepts onto the **wrong chapter blocks**: `2B60`=lip, `2B91`=rectosigmoid, `2B5C`≠stomach, `2C90`=kidney, `2C73`=ovary, `2C77`=**cervix uteri**, `2C80`/`2C8x`=male genital, `2C6Y`=breast, `2D42`=ill-defined sites. SOC started with **0 proc_cpts**.
- **104** MIG-A: 10 in-place recodes + **6 collision-aware MERGEs** into GS/PRS/HBP shared rows (stomach→GS `2B72.Z`, oesophagus→GS `2B70.Z`, melanoma→PRS `2C30.Z`, peritoneal→HBP `2D91`, BCC→PRS `2C32.Z`, SCC-skin→PRS `2C31.Z`). Cross-dept fix: bladder `2C90.0`(RCC code)→`2C94.2` also corrects **UROL**. Three recodes free codes reused for their correct meaning in MIG-D (`2C90.0`→RCC, `2C77.0`→cervix SCC, `2C73.1`→dysgerminoma).
- **105** +4 main_diags (genitourinary, endocrine & adrenal, gynaecological, biliary tract & gallbladder) → 16 categories; linked the orphaned bladder row → genitourinary; moved adrenocortical carcinoma metastatic→endocrine.
- **106+107** +74 diagnoses → **100 total** (42 new rows + 32 links to correctly-labelled shared rows owned by GS/HBP/PRS/ENT/ORTHO/NS/PEDSURG/UROL/OBGYN). 52 embeddings backfilled. Every category ≥5.
- **108+109** Imported **101** AAPC-verified oncology proc_cpts across **18 new alpha groups** (BRST, AXIL, COLR, GAST, ESOG, HEPB, BILI, PANC, THYR, HNCK, SKIN, OVRY, HYST, SARC, NEPH, ADRN, LYMP, METS). None deleted; the 2025-deleted retroperitoneal-tumour codes were avoided (`49186–49190` used).
- **110** Linked all 101 procs + MNR to the 16 main_diags; some dual-linked (HEPB hepatectomy→hepatocellular+metastatic+biliary; THYR→head&neck+endocrine; SKIN→melanoma+non-melanoma; AXIL sentinel-node→breast+melanoma). 93 new proc embeddings backfilled (8 reused shared rows already embedded).

**State after 110: SOC at 100 diagnoses (all ICD-11 verified ✅, all embedded ✅), 102 linked proc_cpts (101 AAPC-verified oncology ops + MNR, all embedded ✅), 16 main_diags; 0 orphans, 0 empty categories. Every category ≥5 diagnoses & ≥6 procs. Audit complete.**

> ⚠️ **Cross-dept mismaps found during SOC audit (flag for those depts)**: MFS uses pancreatic/biliary codes `2C10.0`/`2C10.1`/`2C13.0` for oral/H&N cancers; **GS** labels `2B90.Y` (colon-adeno code) "Lynch syndrome"; **OBGYN** labels `2C76.0` (endometrial code) "carcinoma of uterine cervix". SOC avoided all by substituting free/correctly-labelled codes. Desmoid uses `2F9C` (not ideal `2F7C`, occupied by NS hemangioblastoma mismap). See `MISMAPPED_ICD11_CODES.md`.

### ✅ HBP full dept-audit — COMPLETE (migrations 098–103)
Save-game at `MEDICAL_CODE_AUDITS/HBP/AUDIT_HBP.md`. HBP reference data was **~52% corrupt** (14/27 ICD codes wrong — fabricated sequential codes in the DA9x/DB0x/DC0x/DC1x ranges; portal hypertension in the metabolic chapter `5C81.0`).
- **098** MIG-A: 8 free recodes + **7 collision-aware MERGEs** into existing GS rows (hydatid→`1F73.0`, pseudocyst→`DC30.1`, liver abscess→`DB90.0`, acute cholangitis→`DC13`, choledocholithiasis→`DC11.6`, oesophageal varices→`DA26.0Z`) incl. the cross-dept HCC merge `DA92.0`→`2C12.02` correcting **HBP+SOC**; removed out-of-scope orphan `DA20.3` (oesophageal perforation); relinked pancreatic pseudocyst from "benign liver lesions"→"acute pancreatitis".
- **099+100** +74 diagnoses → **100 total**; 74 embeddings backfilled. Every main_diag ≥5 except metastatic liver disease=4 (documented: few distinct ICD-11 secondary-neoplasm entities exist).
- **101+102** Imported **75** HBP-specific proc_cpts across **7 new groups** (LIVR, BILE, PTBD, PANC additions, SPLN, PORT, ERCP). All AAPC-verified. **Deletion caught**: `47802` (U-tube hepaticoenterostomy, deleted 2025-01-01) excluded. PTBD `47531–47544` is the 2016 replacement set for the deleted percutaneous-biliary codes.
- **103** Linked the 75 new procs + **25 reused GS shared rows** (cholecystectomy 47600/47562, Whipple 48150, partial hepatectomy 47120, ERCP 43260, splenectomy 38100/38120, etc.) + MNR to the 12 main_diags. 75 new proc embeddings backfilled (reused rows already embedded).

**State after 103: HBP at 100 diagnoses (all ICD-11 verified ✅, all embedded ✅), 101 HBP-linked proc_cpts (75 HBP-specific AAPC-verified + 25 reused GS + MNR, all embedded ✅), 12 main_diags; 0 orphans, 0 empty categories. Every main_diag ≥5 diagnoses (except metastatic liver disease=4, documented) & ≥8 procs. Audit complete.**

> ⚠️ **CPT note (reusable)**: the percutaneous transhepatic biliary codes `47510/47511/47525/47530/47560/47561/47630` were DELETED 2016-01-01 → use the `47531–47544` family (cholangiography/drainage/stent/dilation/stone-removal by access type). And `47802` (U-tube hepaticoenterostomy) was DELETED 2025-01-01.
> ⚠️ **Pre-existing MFS mismap noted (flag for MFS audit)**: the seed used `2C10.0` (Adenocarcinoma of pancreas), `2C10.1` (Pancreatic NEN) and `2C12.0` (Malignant neoplasm of liver) as MFS "oral cancer" rows. See `MISMAPPED_ICD11_CODES.md`.

### ✅ PEDSURG full dept-audit — COMPLETE (migrations 091–097)
Save-game at `MEDICAL_CODE_AUDITS/PEDSURG/AUDIT_PEDSURG.md`. PEDSURG data was **~75% corrupt** (18/24 ICD codes wrong — worst yet; surgical conditions scattered into developmental `LA9x`/`LB1x` chapters, tumours in disease chapters, a duodenal-atresia↔pyloric↔Meckel↔annular-pancreas code tangle).
- **091** Added 2 new main_diags (soft tissue & skin lesions; thoracic & lung anomalies) → 15 categories.
- **092** MIG-A: 11 free UPDATEs + **6 collision-aware MERGEs** into shared rows (fournier→PRS `1B71.1`; intussusception→GS `DA91.0`; inguinal→GS `DD51`; umbilical→GS `DD53`; Meckel→GS `LB15.0`; deleted PEDSURG's duplicate `LB13.0`). Resolved duodenal/pyloric cascade, relinked both orphans to soft tissue.
- **093+094** +77 diagnoses → **100 total**; 78 embeddings backfilled. Every main_diag ≥5 except intussusception=3 (documented: only 3 distinct ICD-11 intussusception entities exist).
- **095+096** Imported **101** dept-specific proc_cpts across **13 new alpha groups** (AWAL, APDX, DIAF, ESOP, GUSX, ANOR, HERN, BOWL, NEON, FORG, ONCO, SOFT, THOR). All AAPC-verified. **2023 hernia recode** handled (49570/49572/49580/49585→49591/49592/49593/49613) and **2025 retroperitoneal-tumour deletion** (49203/49204→60540 adrenalectomy + 49186); 46700 adult→46705 infant. 7 THOR codes shared with CTS's existing THOR group (ON CONFLICT reused them).
- **097** Linked all 101 procs to the 15 main_diags + MNR to every category. 94 new proc embeddings backfilled (+7 shared already embedded = all 101).

**State after 097: PEDSURG at 100 diagnoses (all ICD-11 verified ✅, all embedded ✅), 101 dept-specific proc_cpts (all AAPC-verified ✅, all embedded ✅), 15 main_diags; 0 orphans, 0 empty categories. Every main_diag ≥5 diagnoses & ≥5 procs except intussusception (3 dx, documented narrow). Audit complete.**

> ⚠️ **CPT currency notes (reusable)**: (1) the umbilical/epigastric/ventral hernia codes 49560–49590 were DELETED 2023-01-01 → use the anterior-abdominal-hernia family 49591–49622 (by size + reducible/incarcerated + initial/recurrent). (2) intra-abdominal/retroperitoneal tumour excision codes 49203–49205 were DELETED 2025-01-01 → use 49186–49190 (by size) or organ-specific resections (eg adrenalectomy 60540).

### 🔧 dept-audit skill is now resumable (this session)
The `dept-audit` skill now checkpoints continuously so a token-limit interrupt loses no research. New `.claude/skills/dept-audit/resume-check.js` + a **Phase 0** that runs it on every `/dept-audit <DEPT>` and echoes the audit file's `🔄 Progress Checkpoint` block. The audit file is created at **end of Phase 1** (not Phase 6) and updated after every sub-step (2B/2D write in batches of ~10). To resume any interrupted audit: just run `/dept-audit <DEPT>` — Phase 0 shows where to continue.

### ✅ VASC full dept audit — COMPLETE (migrations 085–090)
Save-game at `MEDICAL_CODE_AUDITS/VASC/AUDIT_VASC.md`.
- **085** 18 ICD-11 fixes (VASC data was 61% corrupt) incl. gangrene→`MC85` merge; resolved the 2 long-open codes `BA41.0`→`BD55`, `BD10.4`→`8B22.A`; cross-dept ESRD `GB60.0`→`GB61.5` (TRS+UROL+VASC) and aortic-root `BD50.Z`→`BD50.3Y&XA01A6` (CTS+VASC).
- **086+087** +72 diagnoses → **100 total**, every main_diag ≥5; 84 rows embedded. All ICD-11-verified ✅ + embedded ✅.
- **088+089** Imported **114** dept-specific proc_cpts across **13 new alpha groups** (AORT, EVAR, ENDO, BYPS, ENDA, THRM, PERA, AMPU, DIAL, AVFR, VARX, IVCF, TRMA). All AAPC-verified. **2 deleted codes avoided** (33860→33858, 33870→33871) and the **2026 AMA LER restructure** handled — the lower-extremity endovascular family 37220–37235 was deleted effective 2026-01-01, replaced by new territory codes 37254/37256/37258/37260 (iliac), 37263/37265/37267/37269/37271/37273 (fem-pop), 37280 (tibial), 37296 (inframalleolar). 3 AMPU codes were already ORTHO-owned shared rows (ON CONFLICT reused them).
- **090** Linked all 114 procs to the 12 main_diags + MNR to every category. 111 new proc embeddings backfilled (+3 shared already embedded = all 114).

**State after 090: VASC at 100 diagnoses (all embedded, all ICD-11 verified ✅), 114 dept-specific proc_cpts (all AAPC-verified, all embedded). 12 main_diags; every category ≥5 diagnoses & ≥5 procs; 0 orphans, 0 empty categories. Audit complete.**

> ⚠️ **2026 CPT note (reusable)**: the lower-extremity endovascular revascularization codes 37220–37235 were DELETED by AMA effective 2026-01-01 and replaced by 46 new codes 37254–37299, organised by territory (iliac/fem-pop/tibial-peroneal/inframalleolar) × lesion (stenosis=straightforward vs occlusion=complex) × initial/additional vessel. Use the new codes for any vascular endovascular work going forward.

### Session recap — PRS full dept audit + coverage extension (migrations 078-084)
- **078** Fixed 21 ICD-11 codes (18 wrong + 3 parent→leaf). Original PRS data had injuries/tumours/ulcers in skin-disease `E*` / developmental `L*` chapters. Burns `EJ40/41/42`→`ND92.1/.2/.3` (depth ladder), frostbite `EJ50.0`→`NE41`; cleft `ED00.0/.1`→`LA42.Z`/`LA40.Z`; syndactyly/polydactyly→`LB79.Z`/`LB78.Z`; keloid/hypertrophic→`EE60.0Z`/`EE60.1`; pressure ulcer→`EH90.Z`, diabetic foot→`BD54`; necr. fasc.→`1B71.Z`; BCC/SCC `2F31.0`/`2F33.0`→`2C32.Z`/`2C31.Z`; epidermoid cyst→`EK70.0Z`; GCT-soft-tissue `2B72.0`→`2C35` (cutaneous sarcoma — ideal 2F7C/2F7Z occupied by NS). Two shared rows fixed also benefit **NS** (brachial plexus `NA14.0`→`NA41.Z`) and **PEDSURG** (epidermoid cyst).
- **079** Structural: relinked the recoded scar row (`EH94`) from `contractures` → `scar revision`.
- **080–081** Added 70 diagnoses (30→**100**), every main_diag now ≥5. 86 diagnosis embeddings backfilled.
- **082–083** Imported **100** dept-specific proc_cpts across 12 new alpha groups (FLAP, GRFT, HSGY, CLEF, BURN, BRST, AEST, MICR, WND, SCRV, EXCN, CONT). 100 proc embeddings backfilled.
- **084** Linked all 100 procs to the 12 main_diags + MNR to every category + 4 reused shared procs (BREA 19357/38525, PRPH 64856/64861). All 100 CPTs AAPC-verified (none deleted).

**State after 084: PRS at 100 diagnoses (all embedded, all ICD-11 verified ✅), 100 dept-specific proc_cpts (all AAPC-verified, all embedded). 12 main_diags; every category ≥5 diagnoses & ≥5 procs.**

### Session recap — ORTHO full dept audit + coverage extension (migrations 070-076)
- **070** Fixed 17 ICD-11 codes (+1 leaf refinement): traumatic fractures were in the FB* *disease* chapter instead of N* *injury* chapter (e.g. tibial shaft FB50.0→NC92.2, neck of femur FB80.0→NC72.2Z); cross-chapter mismaps fixed: meniscal tear NC72.0→NC93.3Z, osteomyelitis LA91.1→FB84.Z, OA knee/hip NC90.0/.1→FA01.Z/FA00.Z, rotator cuff tear FA71.0→NC16.0Y.
- **071** Structural: merged 3 mis-coded duplicate rows into NS-owned correct rows (lumbar disc FA30.0→FA80.9, lumbar stenosis FA31.0→FA82, lumbar fx FB20.0→NB52.0); removed a spurious intertrochanteric↔osteonecrosis link. Freed FA30.0, reused for acquired hallux valgus.
- **072–073** Added 73 diagnoses (→105 total). 4 new main_diags created: bone tumours, foot & ankle disorders, hand & wrist disorders, paediatric & developmental conditions. 84 diagnosis embeddings backfilled.
- **074–076** Added 101 dept-specific proc_cpts across 12 new alpha groups (ARTH, SCOP, FIXN, SPIN, HAND, FOOT, SOFT, TUMR, OSTE, PEDS, INFX, AMPU) + linked all to the 17 main_diags with shared MNR. 101 proc embeddings backfilled.
- **077** All 101 CPT codes verified against AAPC (`aapc.com/codes`); 4 corrected: kyphoplasty 22524 (deleted by AMA 2015)→22514, vertebroplasty 22514→22511, SCOP 29891 retitled (osteochondral lesion of talus), TUMR 27065 tightened. 4 rows re-embedded.

**State after 076: ORTHO at 105 diagnoses (all embedded, all ICD-11 verified ✅), 101 dept-specific proc_cpts (all embedded). 17 main_diags; every category ≥5 diagnoses & ≥5 procs EXCEPT `carpal tunnel syndrome` (1 dx, 3 procs — documented single-entity exception).**

### Session recap — GS coverage extension (migrations 066-068)
- **066** Added 15 diagnoses: abdominal trauma (+3: pancreas/small intestine/colon injuries), acute abdomen (+8: mesenteric ischaemia, GI bleed, rectal haemorrhage, anal fistula, rectal prolapse, small bowel intussusception, perianal thrombosis, Meckel diverticulum), appendicitis (+1: perforated with generalised peritonitis), bariatric (+3: metabolic syndrome, NAFLD, NASH)
- **067** Added 17 diagnoses: bowel obstruction (+3: strangulated inguinal hernia, large bowel obstruction, large bowel intussusception), breast lumps & cancer (+8: fibroadenoma, DCIS, ILC, Paget disease, fibrocystic change, gynaecomastia, phyllodes, LCIS), colorectal (+6: rectal adenocarcinoma, anal SCC, GIST, Lynch syndrome, rectosigmoid malignancy, HCC)
- **068** Added 26 diagnoses: cholecystitis & cholelithiasis (+9: choledocholithiasis, cholangitis, pseudocyst, GB polyp, chronic cholecystitis, cholelithiasis+chronic, hepatic abscess, hydatid cyst, pancreatic adenocarcinoma), peptic ulcer disease (+6: GORD, gastric cancer, H. pylori gastritis, GOO, oesophageal varices, oesophageal carcinoma), hernias (+4: epigastric, hiatal, strangulated femoral, strangulated umbilical), thyroid (+7: Graves', multinodular goitre, Hashimoto's, medullary, anaplastic, primary hyperparathyroidism, toxic MNG)
- 50 new embeddings backfilled (8 diagnoses already had embeddings as shared with other depts)
- **069** Added 35 proc_cpts: BILI (percutaneous cholecystostomy, hepaticojejunostomy, choledochoduodenostomy, partial hepatectomy, RFA/MWA ablation, ERCP+stent), PANC (Whipple, PPPD, distal pancreatectomy, pseudocyst drainage, necrosectomy), OESO (transhiatal oesophagectomy, Ivor Lewis, PEG, oesophageal dilation), GASTR (total gastrectomy ×2, Billroth I, Billroth II, duodenal switch), ENDO (variceal ligation, EMR), LAPR (lap liver ablation, lap distal pancreatectomy, lap distal gastrectomy), COLO (total abdominal colectomy, IPAA), NECK (modified radical neck dissection, radical neck+thyroidectomy), BREA (subcutaneous mastectomy for gynaecomastia, breast reconstruction with expander), THYR (parathyroid autotransplantation), ABDO (re-laparotomy, peritoneal abscess drainage, intestinal bypass). All 35 embeddings backfilled.

**State after 069: GS at 96 diagnoses (all embedded, all ICD-11 codes verified ✅), 100 proc_cpts (99 GS-specific + 1 shared). CTS: 100 diagnoses, 100 proc_cpts. NS: 134 diagnoses, 94 proc_cpts.**

**Also from earlier sessions:**
- dept-extend skill merged into dept-audit. dept-extend directory deleted.
- medical-terminologies-mcp configured in user scope; `icd11_search` is the verification tool; `icd11_lookup` is broken (returns "Unknown" for all codes — do not use).

**Pending / next steps:**
1. **GS diagnoses**: 96 total; +4 to reach 100 (minor gaps: Spigelian hernia, chronic appendicitis, diverticular fistula — low priority)
2. **2 VASC ICD-11 codes still open**: `BD10.4` subclavian artery stenosis, `BA41.0` carotid artery stenosis (flagged in `MISMAPPED_ICD11_CODES.md`).
3. ~~**Amblyopia**: correct ICD-11 code for OPHTHAL strabismus category~~ — ✅ RESOLVED 2026-06-27 (migration 141): amblyopia = `9D46` (Impairment of binocular functions).
4. ~~**Proc_cpts for remaining departments**~~ — ✅ DONE. **Every department now has proc_cpts** (UROL imported this session, migrations 149/150; all 15 depts complete: CTS, NS, GS, ORTHO, PRS, VASC, PEDSURG, HBP, SOC, MFS, TRS, OBGYN, ENT, OPHTHAL, UROL).
9. ~~**CA22.Z mislabel** (found during TRS audit): `CA22.Z` is labelled "lung abscess - unspecified" but = COPD unspecified.~~ — ✅ RESOLVED 2026-06-28 (migration 156): CTS row recoded `CA22.Z`→`CA43.2` (Abscess of lung without pneumonia).
5. **NS re-review** (flagged in `MISMAPPED_ICD11_CODES.md`): NS uses `2F7C` for "hemangioblastoma" and `2F7Z` for "epidermoid and dermoid tumors" — both are *uncertain-behaviour neoplasm* codes, likely mismapped. (SOC desmoid had to use `2F9C` instead of the ideal `2F7C` because of this.)
6. ~~**MFS oral-cancer mismap**~~ — ✅ RESOLVED 2026-06-24 (migration 111). The fix also corrected the shared GS `2C10.0` (→pancreatic adeno) and HBP `2C13.0` (→gallbladder adeno) rows.
7. ~~**GS `2B90.Y` mislabel** (found during SOC audit): the colon-adenocarcinoma code `2B90.Y` is labelled "Lynch syndrome" in GS.~~ — ✅ RESOLVED 2026-06-28 (migration 156): recoded `2B90.Y`→`QC61.0` (Family history of malignant neoplasm of digestive organs); ICD-11 has no dedicated Lynch leaf, name "Lynch syndrome" kept.
8. ~~**OBGYN `2C76.0` mislabel**~~ — ✅ RESOLVED 2026-06-27 (migration 124): `2C76.0`→`2C77.Z` (cervix). OBGYN full audit complete.

## 🔴 Handoff note: migrations are now in git (force-added)
`src/migrations/` is in `.gitignore` but all migration files (001–084) were committed with `git add -f`. If `.gitignore` is ever respected again (e.g. `git rm --cached`), migration files would disappear from git. Keep force-adding new migrations.

## Audit/data-quality artifacts
- `MISMAPPED_ICD11_CODES.md` (repo root) — ICD-11 code mismaps found across all departments. ORTHO mismaps resolved 2026-06-19 (migrations 070-071); PRS mismaps resolved 2026-06-20 (migration 078); the 2 long-open VASC codes (`BA41.0`, `BD10.4`) resolved 2026-06-20 (migration 085); PEDSURG (092) + HBP (098) + SOC (104) mismaps resolved 2026-06-23; MFS oral/H&N-cancer mismap resolved 2026-06-24 (migration 111); TRS mismaps resolved 2026-06-27 (migration 117, incl. cross-dept PKD `GB63.0`→`GB81` for TRS+UROL); OBGYN mismaps resolved 2026-06-27 (migration 124, incl. the cervix mismap `2C76.0`→`2C77.Z`); ENT mismaps resolved 2026-06-27 (migration 132, incl. 2 cross-dept MERGEs into MFS `DA04.4` + PEDSURG `DA05.Y`); UROL mismaps resolved 2026-06-27 (migration 145, incl. 7 cross-dept MERGEs into SOC/PEDSURG/TRS/OBGYN rows — RCC/seminoma/varicocele/torsion/VUR/OAB/stress-incont); GS `2B90.Y`="Lynch syndrome"→`QC61.0` and CTS `CA22.Z`="lung abscess"→`CA43.2` resolved 2026-06-28 (migration 156). Open items: only the **NS `2F7C`/`2F7Z` re-review** remains.
- `MEDICAL_CODE_AUDITS/NS/AUDIT_NS.md` — Full audit for NS (ICD-11 + CPT); 10 ICD-11 codes fixed (045); 6 CPT codes fixed (043), 10 CPT title/description updates (044). ✅ **134 diagnoses, 94 proc_cpts — all codes verified. Audit complete.**
- `MEDICAL_CODE_AUDITS/CTS/AUDIT_CTS.md` — Full audit for CTS (ICD-11 + CPT + gaps); 1 name fix (046), 5 new diagnoses (047), 72 new proc_cpts (048–050); extended to 100 diagnoses + 100 proc_cpts (057–059); 52 ICD-11 errors fixed (060–064). ✅ **All codes verified — audit complete.**
- `MEDICAL_CODE_AUDITS/GS/AUDIT_GS.md` — Full audit for GS; 3 new diagnoses (052), 64 new proc_cpts (053–055); 23 ICD-11 errors fixed (065); 58 new diagnoses added (066–068); 35 new proc_cpts added (069). ✅ **96 diagnoses, 100 proc_cpts — all ICD-11 verified. Audit complete.**
- `MEDICAL_CODE_AUDITS/ORTHO/AUDIT_ORTHO.md` — Full audit for ORTHO; 19 ICD-11 errors fixed + 1 leaf refinement (070), 3 duplicate merges + 1 spurious link (071), 73 new diagnoses (072–073), 101 new proc_cpts (074–076), all 101 CPT codes AAPC-verified + 4 corrected (077). ✅ **105 diagnoses, 101 proc_cpts — all ICD-11 and CPT codes verified. Audit complete.**
- `MEDICAL_CODE_AUDITS/PRS/AUDIT_PRS.md` — Full audit for PRS; 21 ICD-11 codes fixed (078, incl. shared NS brachial-plexus + PEDSURG epidermoid rows), 1 structural relink (079), 70 new diagnoses (080–081), 100 new proc_cpts AAPC-verified (082–084). ✅ **100 diagnoses, 100 proc_cpts — all ICD-11 and CPT codes verified. Audit complete.**
- `MEDICAL_CODE_AUDITS/VASC/AUDIT_VASC.md` — Full audit for VASC; data was 61% corrupt. 18 ICD-11 codes fixed (085: incl. cross-dept ESRD fix for TRS+UROL+VASC and aortic-root fix for CTS+VASC); +72 diagnoses (086/087) → 100; +114 proc_cpts AAPC-verified across 13 new alpha groups (088/089), all linked (090); 2 deleted CPTs avoided + 2026 LER restructure handled. ✅ **100 diagnoses, 114 proc_cpts — all ICD-11 and CPT codes verified, all embedded. Audit complete.**
- `MEDICAL_CODE_AUDITS/PEDSURG/AUDIT_PEDSURG.md` — Full audit for PEDSURG; data was ~75% corrupt (worst yet, 18/24 ICD codes wrong). 2 new main_diags (091); 18 ICD fixes incl. 6 collision-aware MERGEs into GS/PRS shared rows + duodenal/pyloric/Meckel cascade (092); +77 diagnoses (093/094) → 100; +101 proc_cpts AAPC-verified across 13 new alpha groups (095/096), all linked (097); 2023 hernia recode + 2025 retro-tumour deletion handled; 7 THOR procs shared with CTS. ✅ **100 diagnoses, 101 proc_cpts — all verified, all embedded. Audit complete (intussusception=3 dx documented narrow).**
- `MEDICAL_CODE_AUDITS/HBP/AUDIT_HBP.md` — Full audit for HBP; data was ~52% corrupt (14/27 ICD codes wrong — fabricated DA9x/DB0x/DC0x sequential codes). 14 ICD fixes incl. 6 collision-aware MERGEs into GS rows + cross-dept HCC merge (HBP+SOC) + orphan removal (098); +74 diagnoses (099/100) → 100; +75 HBP-specific proc_cpts AAPC-verified across 7 new groups (101/102) + 25 reused GS rows, all linked (103); 47802 deletion caught + 2016 percutaneous-biliary recode family used. Discovered the MFS oral-cancer mismap (2C10.0/2C10.1/2C12.0). ✅ **100 diagnoses, 101 proc_cpts — all verified, all embedded. Audit complete (metastatic liver disease=4 dx documented narrow).**
- `MEDICAL_CODE_AUDITS/SOC/AUDIT_SOC.md` — Full audit for SOC (Surgical Oncology); data was ~62% corrupt (13/26 wrong + 3 approx — wrong chapter blocks: 2B60=lip, 2B91=rectosigmoid, 2C90=kidney, 2C73=ovary, 2C77=cervix, 2C6Y=breast, 2D42=ill-defined). 16 code changes incl. 6 collision-aware MERGEs into GS/PRS/HBP rows + cross-dept bladder fix (SOC+UROL) (104); +4 main_diags → 16 + orphan/recategorise structural fixes (105); +74 diagnoses (106/107) → 100; SOC had 0 procs → +101 AAPC-verified oncology proc_cpts across 18 new alpha groups (108/109), all linked (110). Discovered MFS (2C13.0), GS (2B90.Y), OBGYN (2C76.0) mismaps. ✅ **100 diagnoses, 102 proc_cpts — all verified, all embedded. Audit complete.**
- `MEDICAL_CODE_AUDITS/MFS/AUDIT_MFS.md` — Full audit for MFS (Maxillofacial Surgery); **most corrupt yet (~86%, 18/21 wrong)** — facial fractures mis-chaptered into DA0x dental chapter, fabricated DA4x codes, the oral-cancer mismap. 18 fixes incl. 3 MERGEs into PRS clefts/own DA05.0 + **2 cross-dept MERGEs resolving the oral-cancer mismap** (2C10.0→pancreatic adeno fixes GS; 2C13.0→gallbladder adeno fixes HBP) (111); +80 diagnoses (112/113) → 100; MFS had 0 procs → +104 AAPC-verified oral/maxillofacial proc_cpts across 11 alpha groups (114/115) + 10 reused shared (PRS CLEF/EXCN, SOC HNCK), all linked (116); 21310 deletion caught. ✅ **100 diagnoses, 105 proc_cpts — all verified, all embedded. Audit complete.**
- `MEDICAL_CODE_AUDITS/TRS/AUDIT_TRS.md` — Full audit for TRS (Transplant Surgery); **second-most corrupt (~80%, 15/20 wrong)**, started with 20 diagnoses + 0 procs. 15 ICD fixes incl. 3 collision-aware MERGEs (`DB93.1`/`DB96.1Z`/`DB98.5`) + cross-dept PKD `GB63.0`→`GB81` (TRS+UROL) + rejection codes on `NE84`+acuteness extensions (117); +83 diagnoses (118/119/120) → 103; TRS had 0 procs → +100 AAPC-verified transplant proc_cpts across 12 new alpha groups (121/122), all linked (123); DIAL group shared with VASC reused via ON CONFLICT. Found CA22.Z="lung abscess" mislabel (open). ✅ **103 diagnoses, 100 proc_cpts — all verified, all embedded. Audit complete.**
- `MEDICAL_CODE_AUDITS/OBGYN/AUDIT_OBGYN.md` — Full audit for OBGYN (Obstetrics & Gynaecology); data was ~78% corrupt (15/23 wrong + 3 approx — gynae conditions in wrong chapter blocks + obstetric pre-ec/eclampsia/PPH/rupture tangle), started with 0 procs. 18 in-place recodes incl. the resolved **cervix mismap `2C76.0`→`2C77.Z`** (124); +81 diagnoses (125/126/127) → 104; OBGYN had 0 procs → +100 AAPC-verified proc_cpts across 12 new alpha groups (128/129), all linked (130/131); deleted CPT 58823→58820 caught; HYST/ONCO shared with SOC. ✅ **104 diagnoses, 100 proc_cpts — all verified, all embedded. Audit complete.**
- `MEDICAL_CODE_AUDITS/ENT/AUDIT_ENT.md` — Full audit for ENT (Otolaryngology); **least corrupt yet (~14%, 4/29 wrong + 4 approx)** — most seed mismaps already fixed by 026/035. 8 code fixes incl. 2 cross-dept MERGEs (parotid calculus→MFS `DA04.4`; branchial cyst→PEDSURG `DA05.Y`) + OSA `CA62.0`→`7A41` (moved to ch.7 sleep) + 3 parent→leaf + 3 name/AR fixes (132); +73 diagnoses (133/134/135) → 102; ENT had 0 procs → +81 AAPC-verified proc_cpts across 11 new alpha groups (136/137) + **24 reused shared GS/SOC/MFS thyroid/salivary/neck rows**, all linked (138). No new cross-dept mismaps found. ✅ **102 diagnoses, 106 proc_cpts — all verified, all embedded. Audit complete.**
- `MEDICAL_CODE_AUDITS/OPHTHAL/AUDIT_OPHTHAL.md` — Full audit for OPHTHAL (Ophthalmology); data was **~64% corrupt (15/28 wrong + 3 parent→leaf)** — fabricated `9A00/9A20/9A30/9A40/9A50/9A81/9B11/9B20/9B40/9B41` seed scheme. 18 fixes incl. cross-dept MERGE (diabetic retinopathy `9A50.0`→TRS `9B71.0Z`) + conjunctivitis relink eyelid→pterygium (139); +72 diagnoses (140/141) → 100, resolving the open amblyopia item (`9D46`); OPHTHAL had 0 procs → +104 AAPC-verified proc_cpts across 12 new alpha groups (142/143), all linked (144); 2 Cat-III codes (0402T, 0671T) verified active. No new cross-dept mismaps. ✅ **100 diagnoses, 105 proc_cpts — all verified, all embedded. Audit complete.**
- `MEDICAL_CODE_AUDITS/UROL/AUDIT_UROL.md` — Full audit for UROL (Urology); data was **~74% corrupt (16/23 wrong + 1 invalid leaf)** — urological conditions mis-chaptered onto female-genital GA codes, cystitis GC00.x (used for stones), and nephritis GB4x/GB5x blocks + oncology mis-codes (RCC on ovary 2C73, seminoma on vulva 2C70.1). 16 fixes incl. **7 collision-aware MERGEs** into shared SOC/PEDSURG/TRS/OBGYN rows (RCC→`2C90.0`, seminoma→`2C80.2`, varicocele→`BD75.1`, torsion→`GB01.0`, VUR→`GB56.5`, OAB→`GC50.0`, stress-incont→`MF50.20`) + 8 in-place recodes + 1 invalid-leaf BPH `GA90.0`→`GA90` (145); +77 diagnoses (146/147/148) → 100; UROL had 0 procs → +103 AAPC-verified proc_cpts across 13 new alpha groups (149/150), all linked (151); `55700` deleted-CPT (2026) caught & excluded. No new cross-dept mismaps. ✅ **100 diagnoses, 104 proc_cpts — all verified, all embedded. Audit complete.**
